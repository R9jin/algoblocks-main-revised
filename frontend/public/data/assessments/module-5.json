{
  "moduleId": "module-5",
  "moduleTitle": "Dynamic Programming",
  "questions": [
    { "id": "m5_q1", "question": "What is Dynamic Programming?", "options": ["A programming paradigm that uses blocks instead of text", "An optimization technique that solves complex problems by breaking them into overlapping subproblems and storing their results to avoid recomputation", "A brute-force technique that tries all possibilities systematically", "A divide-and-conquer technique that never stores intermediate results"], "answer": 1, "explanation": "Dynamic Programming solves problems with overlapping subproblems and optimal substructure by caching sub-results (memoization or tabulation) to avoid redundant work." },
    { "id": "m5_q2", "question": "What is Memoization?", "options": ["Sorting an array before processing it", "A top-down DP approach where recursive calls cache their results in a table to avoid recomputing the same subproblem", "A bottom-up DP approach that fills a table iteratively from base cases upward", "A technique for compressing memory usage in large arrays"], "answer": 1, "explanation": "Memoization is top-down DP: a recursive function checks if a result is already cached before computing it, storing new results for future calls." },
    { "id": "m5_q3", "question": "What is Tabulation?", "options": ["A top-down recursive approach with caching", "Documenting algorithm steps in a spreadsheet", "A bottom-up DP approach that iteratively fills a table starting from base cases, building up to the target answer", "A technique for sorting data into category tables"], "answer": 2, "explanation": "Tabulation is bottom-up DP: it fills a table iteratively from the smallest subproblems upward, avoiding recursion and stack overflow risks entirely." },
    { "id": "m5_q4", "question": "Is Memoization top-down or bottom-up?", "options": ["Bottom-up — it starts from the base case and builds upward", "Neither — memoization avoids the concept of direction", "Top-down — it starts from the main problem and recursively breaks it into subproblems, caching results", "Both simultaneously"], "answer": 2, "explanation": "Memoization is top-down: the algorithm starts with the full problem, recursively decomposes it, and stores results as subproblems are solved." },
    { "id": "m5_q5", "question": "Is Tabulation top-down or bottom-up?", "options": ["Top-down — it starts from the full problem and recurses", "Neither — tabulation processes problems in random order", "It depends on which direction the table is filled", "Bottom-up — it starts from the smallest base cases and iteratively builds toward the answer"], "answer": 3, "explanation": "Tabulation is bottom-up: it begins with base cases (e.g., fib[0]=0, fib[1]=1) and iteratively fills the table up to the target value." },
    { "id": "m5_q6", "question": "What is the complexity of naive recursive Fibonacci?", "options": ["O(n) — linear", "O(n log n) — linearithmic", "O(2ⁿ) — exponential, due to massive redundant recomputation of subproblems", "O(n²) — quadratic"], "answer": 2, "explanation": "Naive recursive Fibonacci is O(2ⁿ) because it recomputes the same subproblems exponentially — fib(n) calls fib(n-1) and fib(n-2), each of which spawn two more calls, and so on." },
    { "id": "m5_q7", "question": "What is the complexity of DP-optimized Fibonacci?", "options": ["O(2ⁿ) — same as naive recursion", "O(n log n) — due to the caching overhead", "O(n) — linear, since each subproblem is computed exactly once", "O(log n) — because DP halves the problem"], "answer": 2, "explanation": "With memoization or tabulation, each of the n Fibonacci values is computed exactly once and stored, reducing complexity from O(2ⁿ) to O(n)." },
    { "id": "m5_q8", "question": "What are overlapping subproblems?", "options": ["Subproblems that are always independent and never share results", "A property where the same subproblems are encountered and recomputed multiple times during the solution of the main problem", "Subproblems that require sorting before they can be solved", "Subproblems where the answer is always the same regardless of input"], "answer": 1, "explanation": "Overlapping subproblems occur when the recursive decomposition of a problem revisits the same smaller problems multiple times — DP eliminates this redundancy by caching results." },
    { "id": "m5_q9", "question": "What is the 0/1 Knapsack Problem?", "options": ["A problem of making change with minimum coins", "A problem of finding the shortest path in a weighted graph", "A problem of selecting a subset of items with given weights and values to maximize total value without exceeding a weight capacity, where each item is either taken (1) or not (0)", "A problem of sorting items by their value-to-weight ratio"], "answer": 2, "explanation": "0/1 Knapsack asks: given n items each with a weight and value, which items maximize total value while staying within a weight limit? Each item is binary — included or excluded." },
    { "id": "m5_q10", "question": "Are fractional items allowed in 0/1 Knapsack?", "options": ["Yes, items can be split to fill remaining capacity", "Yes, but only for items weighing less than 1 unit", "No, each item must be taken entirely or left entirely — no fractions", "Fractional amounts are automatically rounded to whole numbers"], "answer": 2, "explanation": "The '0/1' in the name means each item is a binary choice: take it completely (1) or don't take it at all (0). This is what prevents a simple greedy solution from working." },
    { "id": "m5_q11", "question": "How does a DP table help in 0/1 Knapsack?", "options": ["It sorts items by value automatically", "It stores the maximum achievable value for every combination of items considered and capacity used, enabling optimal item selection without brute force", "It visualizes the memory usage of the algorithm", "It generates the sorted input required by the greedy approach"], "answer": 1, "explanation": "The DP table dp[i][w] stores the best value achievable using the first i items with weight capacity w — filling it bottom-up avoids recomputing sub-decisions." },
    { "id": "m5_q12", "question": "What is Longest Common Subsequence (LCS)?", "options": ["The longest string that appears contiguously in both input strings", "The longest sequence of characters that appears in the same relative order in both strings, but not necessarily contiguously", "The shortest string needed to encode both inputs", "The string formed by concatenating both inputs and removing duplicates"], "answer": 1, "explanation": "LCS finds the longest sequence present in both strings in the same order — unlike substrings, the characters don't need to be adjacent." },
    { "id": "m5_q13", "question": "Does LCS require letters to be strictly contiguous?", "options": ["Yes, characters must appear consecutively in both strings", "Yes, but only in one of the two strings", "No, the characters must appear in the same relative order but can be non-contiguous", "No, LCS ignores order entirely and just counts shared characters"], "answer": 2, "explanation": "LCS characters must appear in the same relative order in both sequences but can be spread out with gaps between them — this makes LCS distinct from Longest Common Substring." },
    { "id": "m5_q14", "question": "How is the LCS matrix populated?", "options": ["By sorting both strings first, then comparing them linearly", "Cell by cell: if characters match, take the diagonal value + 1; if they don't match, take the maximum of the cell above or to the left", "By finding the midpoint of both strings and expanding outward", "By filling the diagonal first, then propagating values outward"], "answer": 1, "explanation": "The LCS table is filled using the recurrence: if s1[i]==s2[j], dp[i][j]=dp[i-1][j-1]+1; otherwise dp[i][j]=max(dp[i-1][j], dp[i][j-1])." },
    { "id": "m5_q15", "question": "What is the space-time tradeoff?", "options": ["The tradeoff between writing faster code and writing more readable code", "The tradeoff where using more memory to store precomputed results can reduce execution time, or vice versa", "The tradeoff between using loops and recursion", "The tradeoff between time to develop an algorithm and time to run it"], "answer": 1, "explanation": "Space-time tradeoff means spending extra memory (e.g., a DP cache or lookup table) to save computation time — DP is the classic example of accepting O(n) or O(n²) space to avoid exponential recomputation." }
  ]
}
