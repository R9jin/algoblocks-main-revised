{
  "lesson_1": [
    {
      "id": "m3_l1_a1",
      "title": "Recursive Base Case Formulation",
      "task": [
        "Recursion requires a function to invoke itself. To prevent infinite execution and a subsequent memory overflow, the algorithm must implement a terminating condition known as the Base Case.",
        "",
        "Instructions:",
        "1. Define the function `countdown(n)`.",
        "2. Utilize conditional logic to establish the Base Case: if `n <= 0`, return a list containing the string 'Liftoff'.",
        "3. If the Base Case is not met, construct a list combining the current state `n` and the result of the recursive invocation: `countdown(n - 1)`.",
        "4. Return the aggregated list structure."
      ],
      "targetTime": "O(n)",
      "targetSpace": "O(n)",
      "testCasesPool": [
        { "call": "countdown(3)", "expected": "[3, 2, 1, 'Liftoff']", "isHidden": false },
        { "call": "countdown(5)", "expected": "[5, 4, 3, 2, 1, 'Liftoff']", "isHidden": false },
        { "call": "countdown(1)", "expected": "[1, 'Liftoff']", "isHidden": false },
        { "call": "countdown(0)", "expected": "['Liftoff']", "isHidden": true },
        { "call": "countdown(-2)", "expected": "['Liftoff']", "isHidden": true }
      ]
    },
    {
      "id": "m3_l1_a2",
      "title": "Recursive Mathematical Induction",
      "task": [
        "Mathematical induction translates seamlessly into recursive algorithms.",
        "The factorial of a number N is defined recursively as N multiplied by the factorial of N minus 1.",
        "",
        "Instructions:",
        "1. Define the function `factorial(n)`.",
        "2. Establish the Base Case: if `n <= 1`, the function must terminate by returning the integer 1.",
        "3. Formulate the Recursive Step: return the variable `n` multiplied by the invocation of `factorial(n - 1)`.",
        "4. Observe that the algorithm must allocate a new frame on the call stack for every step, yielding an O(n) space complexity."
      ],
      "targetTime": "O(n)",
      "targetSpace": "O(n)",
      "testCasesPool": [
        { "call": "factorial(5)", "expected": "120", "isHidden": false },
        { "call": "factorial(3)", "expected": "6", "isHidden": false },
        { "call": "factorial(1)", "expected": "1", "isHidden": false },
        { "call": "factorial(0)", "expected": "1", "isHidden": true },
        { "call": "factorial(6)", "expected": "720", "isHidden": true }
      ]
    },
    {
      "id": "m3_l1_a3",
      "title": "Recursive Call Stack Visualization",
      "task": [
        "Recursive functions execute in nested hierarchical layers. This depth can be structurally visualized by outputting sequentially nested data arrays.",
        "",
        "Instructions:",
        "1. Define the function `russian_doll(n)`.",
        "2. Implement the Base Case: if `n == 0`, return an empty array `[]`.",
        "3. For the standard execution path, initialize an array containing exactly two elements.",
        "4. Assign the string 'Doll' to the first index. Assign the recursive invocation `russian_doll(n - 1)` to the second index.",
        "5. Return the nested structure."
      ],
      "targetTime": "O(n)",
      "targetSpace": "O(n)",
      "testCasesPool": [
        { "call": "russian_doll(2)", "expected": "['Doll', ['Doll', []]]", "isHidden": false },
        { "call": "russian_doll(3)", "expected": "['Doll', ['Doll', ['Doll', []]]]", "isHidden": false },
        { "call": "russian_doll(1)", "expected": "['Doll', []]", "isHidden": false },
        { "call": "russian_doll(0)", "expected": "[]", "isHidden": true },
        { "call": "russian_doll(4)", "expected": "['Doll', ['Doll', ['Doll', ['Doll', []]]]]", "isHidden": true }
      ]
    },
    {
      "id": "m3_l1_a4",
      "title": "Recursive Pointer Tracking",
      "task": [
        "When evaluating a sequence recursively, passing a sliced sub-array allocates new memory for each function call, degrading performance.",
        "To evaluate a sequence optimally, the algorithm must pass the original memory reference and strictly manage evaluation boundaries utilizing index pointers.",
        "",
        "Instructions:",
        "1. Define the function `is_palindrome(s, left, right)`.",
        "2. Establish the Base Case: if `left >= right`, the convergence is complete. Return `True`.",
        "3. Evaluate the localized boundaries: if `s[left] != s[right]`, return `False`.",
        "4. Execute the Recursive Step: return the boolean result of `is_palindrome(s, left + 1, right - 1)`."
      ],
      "targetTime": "O(n)",
      "targetSpace": "O(n)",
      "testCasesPool": [
        { "call": "is_palindrome('racecar', 0, 6)", "expected": "True", "isHidden": false },
        { "call": "is_palindrome('level', 0, 4)", "expected": "True", "isHidden": false },
        { "call": "is_palindrome('hello', 0, 4)", "expected": "False", "isHidden": false },
        { "call": "is_palindrome('a', 0, 0)", "expected": "True", "isHidden": true },
        { "call": "is_palindrome('', 0, -1)", "expected": "True", "isHidden": true }
      ]
    },
    {
      "id": "m3_l1_a5",
      "title": "Recursive State Accumulation",
      "task": [
        "To aggregate an array recursively without irreversibly altering the original dataset, the algorithm must implement state tracking via an index pointer.",
        "Mutating the input parameter during recursion is considered an unsafe algorithmic side effect.",
        "",
        "Instructions:",
        "1. Define the function `sum_recursive(arr, index)`.",
        "2. Establish the Base Case: if `index == len(arr)`, return `0`.",
        "3. Isolate the current magnitude: retrieve the value at `arr[index]`.",
        "4. Return the current magnitude added to the recursive execution of the subsequent state: `sum_recursive(arr, index + 1)`."
      ],
      "targetTime": "O(n)",
      "targetSpace": "O(n)",
      "testCasesPool": [
        { "call": "sum_recursive([10, 20, 30], 0)", "expected": "60", "isHidden": false },
        { "call": "sum_recursive([5, 5, 5, 5], 0)", "expected": "20", "isHidden": false },
        { "call": "sum_recursive([100], 0)", "expected": "100", "isHidden": false },
        { "call": "sum_recursive([], 0)", "expected": "0", "isHidden": true },
        { "call": "sum_recursive([-10, 10], 0)", "expected": "0", "isHidden": true }
      ]
    }
  ],
  "lesson_2": [
    {
      "id": "m3_l2_a1",
      "title": "Binary Search: Midpoint Evaluation",
      "task": [
        "Sequential evaluation is highly inefficient for strictly sorted data. Binary Search optimizes this process by iteratively evaluating the structural midpoint to halve the search space.",
        "",
        "Instructions:",
        "1. Define the function `binary_search(arr, target)`.",
        "2. Initialize boundary variables: `low = 0` and `high = len(arr) - 1`.",
        "3. Construct a `while` loop bound by the logical condition `low <= high`.",
        "4. Compute the midpoint index utilizing integer division: `mid = (low + high) // 2`.",
        "5. Evaluate equality: if `arr[mid] == target`, return `mid`.",
        "6. Directional logic: if `arr[mid] < target`, adjust the lower boundary `low = mid + 1`. Otherwise, adjust the upper boundary `high = mid - 1`.",
        "7. Return `-1` if the iteration concludes without a match."
      ],
      "targetTime": "O(log n)",
      "targetSpace": "O(1)",
      "testCasesPool": [
        { "call": "binary_search([10, 20, 30, 40], 30)", "expected": "2", "isHidden": false },
        { "call": "binary_search([1, 2, 3, 4], 1)", "expected": "0", "isHidden": false },
        { "call": "binary_search([5, 10, 15], 20)", "expected": "-1", "isHidden": false },
        { "call": "binary_search([], 5)", "expected": "-1", "isHidden": true },
        { "call": "binary_search([7, 14, 21, 28], 14)", "expected": "1", "isHidden": true }
      ]
    },
    {
      "id": "m3_l2_a2",
      "title": "Binary Search: Left Boundary Convergence",
      "task": [
        "A standard implementation may identify any valid match within a sequence of duplicates. To isolate the first occurrence, the algorithm must record the match and force evaluation of the remaining left boundary.",
        "",
        "Instructions:",
        "1. Define the function `binary_first(arr, target)`. Initialize `low`, `high`, and an accumulator `result = -1`.",
        "2. Construct the standard `while` loop and compute the `mid` index.",
        "3. Evaluate equality: if `arr[mid] == target`, assign `result = mid` and restrict the search parameters to the left boundary by updating `high = mid - 1`.",
        "4. If `arr[mid] < target`, update `low = mid + 1`. Otherwise, update `high = mid - 1`.",
        "5. Return the finalized `result` state."
      ],
      "targetTime": "O(log n)",
      "targetSpace": "O(1)",
      "testCasesPool": [
        { "call": "binary_first([1, 2, 2, 2, 3], 2)", "expected": "1", "isHidden": false },
        { "call": "binary_first([5, 5, 5, 6], 5)", "expected": "0", "isHidden": false },
        { "call": "binary_first([1, 2, 3], 3)", "expected": "2", "isHidden": false },
        { "call": "binary_first([], 1)", "expected": "-1", "isHidden": true },
        { "call": "binary_first([7, 7, 7, 7], 7)", "expected": "0", "isHidden": true }
      ]
    },
    {
      "id": "m3_l2_a3",
      "title": "Binary Search: Right Boundary Convergence",
      "task": [
        "To isolate the final occurrence of a duplicate sequence, the algorithmic approach must be inverted to systematically force evaluation of the right boundary.",
        "",
        "Instructions:",
        "1. Define the function `binary_last(arr, target)`. Initialize the required pointers and set `result = -1`.",
        "2. Within the iterative structure, compute the `mid` index.",
        "3. If `arr[mid] == target`, assign `result = mid` and restrict the search parameters to the right boundary by updating `low = mid + 1`.",
        "4. Adjust the `low` and `high` pointers appropriately for non-matching elements.",
        "5. Return the finalized `result` variable."
      ],
      "targetTime": "O(log n)",
      "targetSpace": "O(1)",
      "testCasesPool": [
        { "call": "binary_last([1, 2, 2, 2, 3], 2)", "expected": "3", "isHidden": false },
        { "call": "binary_last([5, 5, 5, 6], 5)", "expected": "2", "isHidden": false },
        { "call": "binary_last([1, 2, 3], 3)", "expected": "2", "isHidden": false },
        { "call": "binary_last([], 1)", "expected": "-1", "isHidden": true },
        { "call": "binary_last([7, 7, 7, 7], 7)", "expected": "3", "isHidden": true }
      ]
    },
    {
      "id": "m3_l2_a4",
      "title": "Binary Search: Insertion Point Location",
      "task": [
        "If a target value is absent from a sorted dataset, the underlying logic of Binary Search can accurately compute its correct sequential insertion index.",
        "When the search space successfully collapses without a match, the lower bound pointer indicates the precise structural position.",
        "",
        "Instructions:",
        "1. Define the function `search_insert(arr, target)`.",
        "2. Execute the standard boundary management logic utilizing `low`, `high`, and `mid` variables.",
        "3. Evaluate equality: if `arr[mid] == target`, return `mid`.",
        "4. If the loop completes without identifying the target, return the `low` pointer variable."
      ],
      "targetTime": "O(log n)",
      "targetSpace": "O(1)",
      "testCasesPool": [
        { "call": "search_insert([1, 3, 5, 6], 5)", "expected": "2", "isHidden": false },
        { "call": "search_insert([1, 3, 5, 6], 2)", "expected": "1", "isHidden": false },
        { "call": "search_insert([1, 3, 5, 6], 7)", "expected": "4", "isHidden": false },
        { "call": "search_insert([1, 3, 5, 6], 0)", "expected": "0", "isHidden": true },
        { "call": "search_insert([], 5)", "expected": "0", "isHidden": true }
      ]
    },
    {
      "id": "m3_l2_a5",
      "title": "Binary Search: Logarithmic Step Counting",
      "task": [
        "To empirically verify the exponential efficiency of spatial halving, we can deploy an internal state counter to track the exact number of iteration cycles required.",
        "",
        "Instructions:",
        "1. Define the function `binary_steps(arr, target)`.",
        "2. Initialize a state variable named `steps` to 0.",
        "3. Inside the `while` loop construct, immediately increment the `steps` variable by 1.",
        "4. Execute standard boundary logic. If the target is identified, return the `steps` variable instead of the index.",
        "5. If the target is absent, return `steps` upon termination of the loop."
      ],
      "targetTime": "O(log n)",
      "targetSpace": "O(1)",
      "testCasesPool": [
        { "call": "binary_steps([1, 2, 3, 4, 5, 6, 7], 4)", "expected": "1", "isHidden": false },
        { "call": "binary_steps([1, 2, 3, 4, 5, 6, 7], 6)", "expected": "2", "isHidden": false },
        { "call": "binary_steps([10, 20, 30], 40)", "expected": "2", "isHidden": false },
        { "call": "binary_steps([], 10)", "expected": "0", "isHidden": true },
        { "call": "binary_steps([100], 100)", "expected": "1", "isHidden": true }
      ]
    }
  ],
  "lesson_3": [
    {
      "id": "m3_l3_a1",
      "title": "Divide Phase: Array Bisection",
      "task": [
        "The preliminary phase of the Divide and Conquer strategy requires structurally dividing large datasets into constituent subproblems.",
        "Merge Sort relies on bisecting an array into two precise halves.",
        "",
        "Instructions:",
        "1. Define the function `halve_array(arr)`.",
        "2. Compute the midpoint index via integer division: `mid = len(arr) // 2`.",
        "3. Utilize array slicing to allocate memory for two new arrays. Assign `left` to index 0 through `mid`.",
        "4. Assign `right` to index `mid` through the end of the array.",
        "5. Return a nested structure containing both subproblems: `[left, right]`."
      ],
      "targetTime": "O(n)",
      "targetSpace": "O(n)",
      "testCasesPool": [
        { "call": "halve_array([1, 2, 3, 4])", "expected": "[[1, 2], [3, 4]]", "isHidden": false },
        { "call": "halve_array([10, 20, 30])", "expected": "[[10], [20, 30]]", "isHidden": false },
        { "call": "halve_array([5])", "expected": "[[], [5]]", "isHidden": false },
        { "call": "halve_array([])", "expected": "[[], []]", "isHidden": true },
        { "call": "halve_array([1, 2, 3, 4, 5])", "expected": "[[1, 2], [3, 4, 5]]", "isHidden": true }
      ]
    },
    {
      "id": "m3_l3_a2",
      "title": "Conquer Phase: Pointer Synchronization",
      "task": [
        "The Conquer phase requires synthesizing two sorted datasets efficiently. This necessitates pointer synchronization.",
        "By tracking the exact index in both arrays simultaneously, elements can be compared and assigned accurately.",
        "",
        "Instructions:",
        "1. Define the function `merge(left, right)`. Initialize an empty `result` auxiliary array.",
        "2. Initialize two pointer variables: `i = 0` for the left array and `j = 0` for the right array.",
        "3. Construct an evaluation loop constrained by `i < len(left)` AND `j < len(right)`.",
        "4. Evaluate the pointers: `if left[i] <= right[j]`. If true, append `left[i]` to `result` and increment pointer `i`.",
        "5. Otherwise, append `right[j]` to `result` and increment pointer `j`.",
        "6. Concatenate any remaining unmerged elements from either array and return the finalized `result`."
      ],
      "targetTime": "O(n)",
      "targetSpace": "O(n)",
      "testCasesPool": [
        { "call": "merge([1, 3], [2, 4])", "expected": "[1, 2, 3, 4]", "isHidden": false },
        { "call": "merge([5, 9], [1, 8])", "expected": "[1, 5, 8, 9]", "isHidden": false },
        { "call": "merge([10], [])", "expected": "[10]", "isHidden": false },
        { "call": "merge([], [5])", "expected": "[5]", "isHidden": true },
        { "call": "merge([1, 1], [1, 1])", "expected": "[1, 1, 1, 1]", "isHidden": true }
      ]
    },
    {
      "id": "m3_l3_a3",
      "title": "Recursive Orchestration: Merge Sort",
      "task": [
        "Merge Sort recursively divides the array structure until the absolute base elements are isolated. The helper function is then invoked to merge the hierarchical branches sequentially.",
        "",
        "Instructions:",
        "1. Define the function `merge_sort(arr)`. Ensure your previous `merge()` function is accessible in the workspace.",
        "2. Implement the Base Case: if `len(arr) <= 1`, return the array structure unmodified.",
        "3. Compute the current bisection index: `mid = len(arr) // 2`.",
        "4. Apply recursive depth parameters: Assign `left = merge_sort(arr[:mid])` and `right = merge_sort(arr[mid:])`.",
        "5. Conclude execution by returning the synthesized operation: `merge(left, right)`."
      ],
      "targetTime": "O(n log n)",
      "targetSpace": "O(n)",
      "testCasesPool": [
        { "call": "merge_sort([4, 2, 3, 1])", "expected": "[1, 2, 3, 4]", "isHidden": false },
        { "call": "merge_sort([38, 27, 43, 3, 9, 82, 10])", "expected": "[3, 9, 10, 27, 38, 43, 82]", "isHidden": false },
        { "call": "merge_sort([5, 5, 5])", "expected": "[5, 5, 5]", "isHidden": false },
        { "call": "merge_sort([])", "expected": "[]", "isHidden": true },
        { "call": "merge_sort([10, -2, 8])", "expected": "[-2, 8, 10]", "isHidden": true }
      ]
    },
    {
      "id": "m3_l3_a4",
      "title": "Merge Sort: Descending Implementation",
      "task": [
        "Inverting the sequencing order of Merge Sort does not require structural modifications to the recursive framework.",
        "The algorithmic adjustment is localized entirely within the comparison operators of the merge phase.",
        "",
        "Instructions:",
        "1. Define the function `merge_sort_desc(arr)` containing an inner `merge_desc(left, right)` helper function.",
        "2. Within the localized helper function, modify the comparison logic to evaluate `if left[i] >= right[j]`.",
        "3. If the evaluation is true, append data from the `left` array and increment pointer `i`. Otherwise, append from `right` and increment `j`.",
        "4. Synthesize the recursive branches exactly as demonstrated in the ascending variant."
      ],
      "targetTime": "O(n log n)",
      "targetSpace": "O(n)",
      "testCasesPool": [
        { "call": "merge_sort_desc([1, 2, 3])", "expected": "[3, 2, 1]", "isHidden": false },
        { "call": "merge_sort_desc([5, 1, 4])", "expected": "[5, 4, 1]", "isHidden": false },
        { "call": "merge_sort_desc([7, 7, 7])", "expected": "[7, 7, 7]", "isHidden": false },
        { "call": "merge_sort_desc([])", "expected": "[]", "isHidden": true },
        { "call": "merge_sort_desc([-5, -1, 0])", "expected": "[0, -1, -5]", "isHidden": true }
      ]
    },
    {
      "id": "m3_l3_a5",
      "title": "Merge Sort: Lexicographical Evaluation",
      "task": [
        "Merge Sort maintains high consistency across data types. High-level comparative operators natively process string objects, allowing identical recursive architecture to produce lexicographical sequencing.",
        "",
        "Instructions:",
        "1. Define the function `merge_strings(arr)`.",
        "2. Replicate the standard ascending `merge_sort` logic.",
        "3. The comparative operators will analyze character data to output an alphabetized array without modification."
      ],
      "targetTime": "O(n log n)",
      "targetSpace": "O(n)",
      "testCasesPool": [
        { "call": "merge_strings(['c', 'a', 'b'])", "expected": "['a', 'b', 'c']", "isHidden": false },
        { "call": "merge_strings(['z', 'x', 'y'])", "expected": "['x', 'y', 'z']", "isHidden": false },
        { "call": "merge_strings(['apple', 'banana', 'ant'])", "expected": "['ant', 'apple', 'banana']", "isHidden": false },
        { "call": "merge_strings([])", "expected": "[]", "isHidden": true },
        { "call": "merge_strings(['a'])", "expected": "['a']", "isHidden": true }
      ]
    }
  ],
  "lesson_4": [
    {
      "id": "m3_l4_a1",
      "title": "Quick Sort: Pivot Partitioning",
      "task": [
        "The Quick Sort algorithm optimizes performance by selecting a pivot element and systematically dividing data based on value rather than pure bisection.",
        "",
        "Instructions:",
        "1. Define the function `partition(arr)`. Evaluate the base case, returning `[]` if the array is empty.",
        "2. Isolate the comparison basis: assign `pivot = arr[0]`. Initialize two arrays: `left` and `right`.",
        "3. Construct a sequence loop iterating over the remaining values (`arr[1:]`).",
        "4. Evaluate magnitude: if a value is strictly less than the pivot, append it to `left`. Otherwise, append it to `right`.",
        "5. Return a synthesized nested representation: `[left, pivot, right]`."
      ],
      "targetTime": "O(n)",
      "targetSpace": "O(n)",
      "testCasesPool": [
        { "call": "partition([5, 1, 8, 2])", "expected": "[[1, 2], 5, [8]]", "isHidden": false },
        { "call": "partition([10, 20, 5])", "expected": "[[5], 10, [20]]", "isHidden": false },
        { "call": "partition([3, 3, 3])", "expected": "[[], 3, [3, 3]]", "isHidden": false },
        { "call": "partition([])", "expected": "[]", "isHidden": true },
        { "call": "partition([7])", "expected": "[[], 7, []]", "isHidden": true }
      ]
    },
    {
      "id": "m3_l4_a2",
      "title": "Recursive Orchestration: Quick Sort",
      "task": [
        "Applying recursive depth to the partitioning logic permits the left and right sub-arrays to be sequentially sorted prior to final string concatenation.",
        "",
        "Instructions:",
        "1. Define the function `quick_sort(arr)`.",
        "2. Evaluate the structural boundary: if `len(arr) <= 1`, return `arr`.",
        "3. Isolate `pivot = arr[0]`. Apply partitioning principles to derive the `left` and `right` sequence arrays.",
        "4. Synthesize the recursive execution: return `quick_sort(left) + [pivot] + quick_sort(right)`."
      ],
      "targetTime": "O(n log n)",
      "targetSpace": "O(n)",
      "testCasesPool": [
        { "call": "quick_sort([3, 2, 1, 4])", "expected": "[1, 2, 3, 4]", "isHidden": false },
        { "call": "quick_sort([10, 7, 8, 9, 1, 5])", "expected": "[1, 5, 7, 8, 9, 10]", "isHidden": false },
        { "call": "quick_sort([5, 5, 5])", "expected": "[5, 5, 5]", "isHidden": false },
        { "call": "quick_sort([])", "expected": "[]", "isHidden": true },
        { "call": "quick_sort([1])", "expected": "[1]", "isHidden": true }
      ]
    },
    {
      "id": "m3_l4_a3",
      "title": "Quick Sort: Descending Partitioning",
      "task": [
        "Modifying Quick Sort for descending iteration requires an inversion of the primary conditional evaluation during array distribution.",
        "",
        "Instructions:",
        "1. Define the function `quick_sort_desc(arr)`.",
        "2. Retain the structural boundary parameter: if `len(arr) <= 1`, return `arr`.",
        "3. Isolate `pivot = arr[0]`. Construct an iterative sequence across `arr[1:]`.",
        "4. Invert distribution bounds: if a value is strictly greater than the pivot, append it to the `left` array. Otherwise, append to `right`.",
        "5. Conclude recursive orchestration: return `quick_sort_desc(left) + [pivot] + quick_sort_desc(right)`."
      ],
      "targetTime": "O(n log n)",
      "targetSpace": "O(n)",
      "testCasesPool": [
        { "call": "quick_sort_desc([1, 2, 3])", "expected": "[3, 2, 1]", "isHidden": false },
        { "call": "quick_sort_desc([5, 1, 4, 8])", "expected": "[8, 5, 4, 1]", "isHidden": false },
        { "call": "quick_sort_desc([7, 7, 7])", "expected": "[7, 7, 7]", "isHidden": false },
        { "call": "quick_sort_desc([])", "expected": "[]", "isHidden": true },
        { "call": "quick_sort_desc([-5, 0, 5])", "expected": "[5, 0, -5]", "isHidden": true }
      ]
    },
    {
      "id": "m3_l4_a4",
      "title": "Quick Sort: Lexicographical Partitioning",
      "task": [
        "Quick Sort maintains highly effective algorithmic efficiency when evaluating textual datasets, leveraging underlying ASCII interpretation.",
        "",
        "Instructions:",
        "1. Define the function `quick_sort_strings(arr)`.",
        "2. Implement the standard ascending `quick_sort` architecture formulated previously.",
        "3. Standard comparison operators will sequentially evaluate character data to distribute nodes appropriately against the current string pivot."
      ],
      "targetTime": "O(n log n)",
      "targetSpace": "O(n)",
      "testCasesPool": [
        { "call": "quick_sort_strings(['delta', 'alpha', 'charlie'])", "expected": "['alpha', 'charlie', 'delta']", "isHidden": false },
        { "call": "quick_sort_strings(['z', 'a', 'm'])", "expected": "['a', 'm', 'z']", "isHidden": false },
        { "call": "quick_sort_strings(['same', 'same'])", "expected": "['same', 'same']", "isHidden": false },
        { "call": "quick_sort_strings([])", "expected": "[]", "isHidden": true },
        { "call": "quick_sort_strings(['b'])", "expected": "['b']", "isHidden": true }
      ]
    },
    {
      "id": "m3_l4_a5",
      "title": "Quickselect: Kth Element Isolation",
      "task": [
        "The recursive logic from Quick Sort can be optimized to isolate the Kth smallest element mathematically without exhaustively sorting the entire sequence.",
        "By discarding the irrelevant partition, this algorithm achieves an average time complexity of O(n).",
        "",
        "Instructions:",
        "1. Define the function `quickselect(arr, k)`.",
        "2. Execute the base partitioning mechanism to isolate the `left` array, `pivot`, and `right` array.",
        "3. Evaluate magnitude: calculate the size of the `left` array.",
        "4. If `k` exactly matches the length of the `left` array plus 1, the `pivot` holds the correct value. Return it.",
        "5. If `k` is less than or equal to the left length, the target is located there. Return `quickselect(left, k)`.",
        "6. If the target resides in the right partition, recalibrate `k`. Return `quickselect(right, k - len(left) - 1)`."
      ],
      "targetTime": "O(n)",
      "targetSpace": "O(n)",
      "testCasesPool": [
        { "call": "quickselect([3, 2, 1, 5, 4], 2)", "expected": "2", "isHidden": false },
        { "call": "quickselect([7, 10, 4, 3, 20, 15], 3)", "expected": "10", "isHidden": false },
        { "call": "quickselect([7, 10, 4, 3, 20, 15], 4)", "expected": "15", "isHidden": false },
        { "call": "quickselect([1], 1)", "expected": "1", "isHidden": true }
      ]
    }
  ],
  "optimizations": [
    {
      "id": "m3_opt_1",
      "title": "Optimization Challenge: Logarithmic Search",
      "task": [
        "A linear sequential scan through a pre-sorted dataset operates under a constrained O(n) time complexity.",
        "Refactoring the iterative traversal into a bounded bisection architecture shifts the time evaluation to an optimal O(log n) scaling limit.",
        "",
        "Optimization Guidelines:",
        "1. Evaluate the provided structure. Delete the linear `for` loop implementation entirely.",
        "2. Initialize boundary tracking variables: `low` set to 0, and `high` set to the terminal index.",
        "3. Apply a structural `while` loop dependent on the `low <= high` mathematical condition.",
        "4. Calculate the midpoint index iteratively. Evaluate the pointer equality and reassign boundaries to continuously halve the evaluation space.",
        "5. Observe the analyzer verify the successful translation to an O(log n) bounded complexity."
      ],
      "targetTime": "O(log n)",
      "targetSpace": "O(1)",
      "templateUrl": "/data/optimizations/m3-3.1.json",
      "testCasesPool": [
        { "call": "search([1, 3, 5, 7, 9], 7)", "expected": "3", "isHidden": false },
        { "call": "search([10, 20, 30, 40], 10)", "expected": "0", "isHidden": false },
        { "call": "search([2, 4, 6], 5)", "expected": "-1", "isHidden": false },
        { "call": "search([], 1)", "expected": "-1", "isHidden": true },
        { "call": "search([5], 5)", "expected": "0", "isHidden": true }
      ]
    },
    {
      "id": "m3_opt_2",
      "title": "Optimization Challenge: Gradient Convergence",
      "task": [
        "A peak element represents a value strictly greater than its neighbors. An exhaustive evaluation enforces an O(n) sequential loop.",
        "We can deploy a structurally modified binary search to continuously follow the mathematical gradient uphill, resolving the evaluation in O(log n) time.",
        "",
        "Optimization Guidelines:",
        "1. Remove the inefficient O(n) traversal structure.",
        "2. Establish `low` and `high` pointers to dictate bounds.",
        "3. Implement a sequence loop running while `low < high`.",
        "4. Evaluate the local gradient: if `arr[mid] < arr[mid + 1]`, the slope ascends towards the right partition. Adjust the `low` pointer to `mid + 1`.",
        "5. If the gradient descends, the peak exists within the left bounds. Constrain the `high` pointer strictly to `mid`.",
        "6. Return the index of the `low` variable upon loop termination."
      ],
      "targetTime": "O(log n)",
      "targetSpace": "O(1)",
      "templateUrl": "/data/optimizations/m3-3.2.json",
      "testCasesPool": [
        { "call": "find_peak([1, 2, 3, 1])", "expected": "2", "isHidden": false },
        { "call": "find_peak([1, 2, 1, 3, 5, 6, 4])", "expected": "5", "isHidden": false },
        { "call": "find_peak([1, 2, 3, 4, 5])", "expected": "4", "isHidden": false },
        { "call": "find_peak([10, 9, 8, 7])", "expected": "0", "isHidden": true },
        { "call": "find_peak([5])", "expected": "0", "isHidden": true }
      ]
    },
    {
      "id": "m3_opt_3",
      "title": "Optimization Challenge: Iterative Space Reduction",
      "task": [
        "A recursive implementation of Binary Search allocates a new functional frame on the call stack for every recursive call, resulting in an O(log n) auxiliary space complexity.",
        "By converting the recursive logic into an iterative structure, the algorithm tracks evaluation boundaries within a single memory frame, achieving an optimal O(1) space complexity.",
        "",
        "Optimization Guidelines:",
        "1. Isolate the memory stack generated by returning the search function recursively.",
        "2. Remove the recursive invocations from the code logic.",
        "3. Refactor the core evaluation utilizing an iterative `while low <= high:` loop structure.",
        "4. Reassign the `low` and `high` pointer variables directly within the operational loop rather than passing them as parameter inputs.",
        "5. Confirm via the analytical readout that the Space Complexity metric has reduced to O(1)."
      ],
      "targetTime": "O(log n)",
      "targetSpace": "O(1)",
      "templateUrl": "/data/optimizations/m3-3.3.json",
      "testCasesPool": [
        { "call": "search_optimized([10, 20, 30, 40, 50], 40)", "expected": "3", "isHidden": false },
        { "call": "search_optimized([1, 2, 3], 2)", "expected": "1", "isHidden": false },
        { "call": "search_optimized([5, 8, 12], 20)", "expected": "-1", "isHidden": false },
        { "call": "search_optimized([], 5)", "expected": "-1", "isHidden": true },
        { "call": "search_optimized([7], 7)", "expected": "0", "isHidden": true }
      ]
    }
  ]
}