{
  "lesson_1": [
    {
      "id": "m3_l1_a1",
      "title": "The Base Case (Countdown)",
      "task": [
        "Recursion is a neat trick where a function calls *itself*! But it must know when to stop, otherwise it runs forever. That stopping point is called the 'Base Case'. Let's build a rocket countdown.",
        "",
        "Instructions:",
        "1. Open the 'Functions' tab and define `countdown(n)`.",
        "2. Open the 'Logic' tab and grab an 'If/Else' block. This is our Base Case: If `n <= 0`, return a list containing just the text `'Liftoff!'`.",
        "3. In the 'Else' section (when `n` is greater than 0), we want to join two lists together using the 'join list' block from the 'Lists (Built-in Type)' tab.",
        "4. In the first slot, put a list containing just `n`. In the second slot, call `countdown(n - 1)`.",
        "5. Return the joined list.",
        "",
        "Sample Outputs:",
        "countdown(3) → [3, 2, 1, 'Liftoff!']",
        "countdown(1) → [1, 'Liftoff!']"
      ],
      "targetTime": "O(n)",
      "targetSpace": "O(n)",
      "testCasesPool": [
        { "call": "countdown(3)", "expected": "[3, 2, 1, 'Liftoff!']", "isHidden": false },
        { "call": "countdown(5)", "expected": "[5, 4, 3, 2, 1, 'Liftoff!']", "isHidden": false },
        { "call": "countdown(1)", "expected": "[1, 'Liftoff!']", "isHidden": false },
        { "call": "countdown(0)", "expected": "['Liftoff!']", "isHidden": true },
        { "call": "countdown(-2)", "expected": "['Liftoff!']", "isHidden": true }
      ]
    },
    {
      "id": "m3_l1_a2",
      "title": "Building a Staircase",
      "task": [
        "Let's see how the recursive stack builds up by drawing a staircase of stars! Each time the function calls itself, it adds a new step.",
        "",
        "Instructions:",
        "1. Define `build_stairs(n)`.",
        "2. Base Case: If `n == 0`, just return an empty list `[]`.",
        "3. Else, use the 'join list' block to combine `build_stairs(n - 1)` with a list containing your new step.",
        "4. For your new step, go to the 'Text' tab and use the 'repeat text times' block to repeat `'*'` exactly `n` times.",
        "5. Return the combined list.",
        "",
        "Sample Outputs:",
        "build_stairs(3) → ['*', '**', '***']",
        "build_stairs(1) → ['*']"
      ],
      "targetTime": "O(n)",
      "targetSpace": "O(n)",
      "testCasesPool": [
        { "call": "build_stairs(3)", "expected": "['*', '**', '***']", "isHidden": false },
        { "call": "build_stairs(5)", "expected": "['*', '**', '***', '****', '*****']", "isHidden": false },
        { "call": "build_stairs(1)", "expected": "['*']", "isHidden": false },
        { "call": "build_stairs(0)", "expected": "[]", "isHidden": true },
        { "call": "build_stairs(2)", "expected": "['*', '**']", "isHidden": true }
      ]
    },
    {
      "id": "m3_l1_a3",
      "title": "The Russian Doll",
      "task": [
        "Recursion goes deeper and deeper. Let's visualize this by putting a list inside a list inside a list... just like nesting Russian nesting dolls!",
        "",
        "Instructions:",
        "1. Define `russian_doll(n)`.",
        "2. Base case: If `n == 0`, return an empty list `[]`.",
        "3. Else, use the 'create list with' block to make a list with exactly 2 items.",
        "4. In the first slot, put the text `'Doll'`. In the second slot, call `russian_doll(n - 1)`.",
        "5. Return this new nested list.",
        "",
        "Sample Outputs:",
        "russian_doll(2) → ['Doll', ['Doll', []]]",
        "russian_doll(1) → ['Doll', []]"
      ],
      "targetTime": "O(n)",
      "targetSpace": "O(n)",
      "testCasesPool": [
        { "call": "russian_doll(2)", "expected": "['Doll', ['Doll', []]]", "isHidden": false },
        { "call": "russian_doll(3)", "expected": "['Doll', ['Doll', ['Doll', []]]]", "isHidden": false },
        { "call": "russian_doll(1)", "expected": "['Doll', []]", "isHidden": false },
        { "call": "russian_doll(0)", "expected": "[]", "isHidden": true },
        { "call": "russian_doll(4)", "expected": "['Doll', ['Doll', ['Doll', ['Doll', []]]]]", "isHidden": true }
      ]
    },
    {
      "id": "m3_l1_a4",
      "title": "Mirror Words (Palindromes)",
      "task": [
        "A palindrome is a word spelled the same forwards and backwards. We can use recursion to check the outside letters, then shrink the word and check the inside!",
        "",
        "Instructions:",
        "1. Define `is_palindrome(s)`.",
        "2. Base Case: If the length of the string `s` is less than or equal to 1, return `True` (a single letter is always a mirror!).",
        "3. Check the outside: If the first letter (`s[0]`) does NOT equal the last letter (`s[-1]`), return `False`.",
        "4. The Recursive Step: Return `is_palindrome` on the inside of the string (slice from index 1 to -1).",
        "",
        "Sample Outputs:",
        "is_palindrome('racecar') → True",
        "is_palindrome('hello') → False"
      ],
      "targetTime": "O(n)",
      "targetSpace": "O(n)",
      "testCasesPool": [
        { "call": "is_palindrome('racecar')", "expected": "True", "isHidden": false },
        { "call": "is_palindrome('level')", "expected": "True", "isHidden": false },
        { "call": "is_palindrome('hello')", "expected": "False", "isHidden": false },
        { "call": "is_palindrome('a')", "expected": "True", "isHidden": true },
        { "call": "is_palindrome('')", "expected": "True", "isHidden": true }
      ]
    },
    {
      "id": "m3_l1_a5",
      "title": "Recursive Addition",
      "task": [
        "Let's add numbers in a list without using a loop! We can pop the last number off, and add it to a recursive call of the remaining list.",
        "",
        "Instructions:",
        "1. Define `sum_recursive(arr)`.",
        "2. Base Case: If the list is empty (`len(arr) == 0`), return `0`.",
        "3. Use the 'remove and get last item' block from the 'Lists' tab to pull the last number out and save it in a variable called `last_num`.",
        "4. Return `last_num` + `sum_recursive(arr)`.",
        "",
        "Sample Outputs:",
        "sum_recursive([10, 20, 30]) → 60",
        "sum_recursive([5, 5]) → 10"
      ],
      "targetTime": "O(n)",
      "targetSpace": "O(n)",
      "testCasesPool": [
        { "call": "sum_recursive([10, 20, 30])", "expected": "60", "isHidden": false },
        { "call": "sum_recursive([5, 5, 5, 5])", "expected": "20", "isHidden": false },
        { "call": "sum_recursive([100])", "expected": "100", "isHidden": false },
        { "call": "sum_recursive([])", "expected": "0", "isHidden": true },
        { "call": "sum_recursive([-10, 10])", "expected": "0", "isHidden": true }
      ]
    }
  ],
  "lesson_2": [
    {
      "id": "m3_l2_a1",
      "title": "Halving the Search (Binary Search)",
      "task": [
        "If a list is perfectly sorted, you don't need to check every item! You can check the exact middle. If the target is smaller, you throw away the entire right half of the list. This is incredibly fast!",
        "",
        "Instructions:",
        "1. Define `binary_search(arr, target)`.",
        "2. Create two pointer variables: `low = 0` and `high = len(arr) - 1`.",
        "3. Make a `while` loop that runs as long as `low <= high`.",
        "4. Inside, calculate the middle: `mid = (low + high) // 2` (use floor division!).",
        "5. If `arr[mid] == target`, return `mid`. If `arr[mid] < target`, change `low` to `mid + 1`. Otherwise, change `high` to `mid - 1`.",
        "6. Return `-1` at the very end.",
        "",
        "Sample Outputs:",
        "binary_search([10, 20, 30, 40], 30) → 2",
        "binary_search([1, 2, 3, 4], 1) → 0"
      ],
      "targetTime": "O(log n)",
      "targetSpace": "O(1)",
      "testCasesPool": [
        { "call": "binary_search([10, 20, 30, 40], 30)", "expected": "2", "isHidden": false },
        { "call": "binary_search([1, 2, 3, 4], 1)", "expected": "0", "isHidden": false },
        { "call": "binary_search([5, 10, 15], 20)", "expected": "-1", "isHidden": false },
        { "call": "binary_search([], 5)", "expected": "-1", "isHidden": true },
        { "call": "binary_search([7, 14, 21, 28], 14)", "expected": "1", "isHidden": true }
      ]
    },
    {
      "id": "m3_l2_a2",
      "title": "Finding the First Duplicate",
      "task": [
        "If a list has duplicates (like `[5, 5, 5]`), standard binary search might land on the middle `5`. We need to adjust it to keep searching the left half to find the *very first* one.",
        "",
        "Instructions:",
        "1. Define `binary_first(arr, target)`. Set `low = 0`, `high = len(arr) - 1`, and `result = -1`.",
        "2. Use the same `while low <= high` loop and calculate `mid`.",
        "3. If `arr[mid] == target`, don't return yet! Save `result = mid` and then force the search left by setting `high = mid - 1`.",
        "4. Otherwise, shift `low` or `high` normally just like the last task.",
        "5. Return `result` at the end.",
        "",
        "Sample Outputs:",
        "binary_first([1, 2, 2, 2, 3], 2) → 1",
        "binary_first([5, 5, 5, 6], 5) → 0"
      ],
      "targetTime": "O(log n)",
      "targetSpace": "O(1)",
      "testCasesPool": [
        { "call": "binary_first([1, 2, 2, 2, 3], 2)", "expected": "1", "isHidden": false },
        { "call": "binary_first([5, 5, 5, 6], 5)", "expected": "0", "isHidden": false },
        { "call": "binary_first([1, 2, 3], 3)", "expected": "2", "isHidden": false },
        { "call": "binary_first([], 1)", "expected": "-1", "isHidden": true },
        { "call": "binary_first([7, 7, 7, 7], 7)", "expected": "0", "isHidden": true }
      ]
    },
    {
      "id": "m3_l2_a3",
      "title": "Finding the Last Duplicate",
      "task": [
        "Finding the last duplicate is the exact opposite. When we find a match, we save the spot but keep searching the *right* half just in case there's another one later!",
        "",
        "Instructions:",
        "1. Define `binary_last(arr, target)`. Initialize your pointers and `result = -1`.",
        "2. Inside the `while` loop, calculate `mid`.",
        "3. If `arr[mid] == target`, save `result = mid` and then force the search right by setting `low = mid + 1`.",
        "4. Otherwise, adjust `low` and `high` normally.",
        "5. Return `result`.",
        "",
        "Sample Outputs:",
        "binary_last([1, 2, 2, 2, 3], 2) → 3",
        "binary_last([5, 5, 5, 6], 5) → 2"
      ],
      "targetTime": "O(log n)",
      "targetSpace": "O(1)",
      "testCasesPool": [
        { "call": "binary_last([1, 2, 2, 2, 3], 2)", "expected": "3", "isHidden": false },
        { "call": "binary_last([5, 5, 5, 6], 5)", "expected": "2", "isHidden": false },
        { "call": "binary_last([1, 2, 3], 3)", "expected": "2", "isHidden": false },
        { "call": "binary_last([], 1)", "expected": "-1", "isHidden": true },
        { "call": "binary_last([7, 7, 7, 7], 7)", "expected": "3", "isHidden": true }
      ]
    },
    {
      "id": "m3_l2_a4",
      "title": "Where Does It Belong?",
      "task": [
        "If a number isn't in a sorted list, we can use halving to figure out exactly where it *should* be inserted to keep the list in order.",
        "",
        "Instructions:",
        "1. Define `search_insert(arr, target)`.",
        "2. Run a normal Binary Search with `low`, `high`, and `mid`.",
        "3. If `arr[mid] == target`, return `mid`.",
        "4. If the loop completely finishes and you never found the target, simply return the `low` variable. That variable naturally points to the exact perfect insertion spot!",
        "",
        "Sample Outputs:",
        "search_insert([1, 3, 5, 6], 5) → 2",
        "search_insert([1, 3, 5, 6], 2) → 1"
      ],
      "targetTime": "O(log n)",
      "targetSpace": "O(1)",
      "testCasesPool": [
        { "call": "search_insert([1, 3, 5, 6], 5)", "expected": "2", "isHidden": false },
        { "call": "search_insert([1, 3, 5, 6], 2)", "expected": "1", "isHidden": false },
        { "call": "search_insert([1, 3, 5, 6], 7)", "expected": "4", "isHidden": false },
        { "call": "search_insert([1, 3, 5, 6], 0)", "expected": "0", "isHidden": true },
        { "call": "search_insert([], 5)", "expected": "0", "isHidden": true }
      ]
    },
    {
      "id": "m3_l2_a5",
      "title": "Counting the Steps",
      "task": [
        "Just how incredibly fast is halving the search? Let's add a counter to see exactly how many jumps it takes to find an answer.",
        "",
        "Instructions:",
        "1. Define `binary_steps(arr, target)`.",
        "2. Create a variable called `steps` and set it to 0.",
        "3. Inside your `while` loop, immediately add 1 to `steps`.",
        "4. Run your normal binary search logic. But if you find the target, return `steps` instead of the index.",
        "5. If the loop ends and nothing was found, return `steps`.",
        "",
        "Sample Outputs:",
        "binary_steps([1, 2, 3, 4, 5, 6, 7], 4) → 1",
        "binary_steps([1, 2, 3, 4, 5, 6, 7], 6) → 2"
      ],
      "targetTime": "O(log n)",
      "targetSpace": "O(1)",
      "testCasesPool": [
        { "call": "binary_steps([1, 2, 3, 4, 5, 6, 7], 4)", "expected": "1", "isHidden": false },
        { "call": "binary_steps([1, 2, 3, 4, 5, 6, 7], 6)", "expected": "2", "isHidden": false },
        { "call": "binary_steps([10, 20, 30], 40)", "expected": "2", "isHidden": false },
        { "call": "binary_steps([], 10)", "expected": "0", "isHidden": true },
        { "call": "binary_steps([100], 100)", "expected": "1", "isHidden": true }
      ]
    }
  ],
  "lesson_3": [
    {
      "id": "m3_l3_a1",
      "title": "Splitting in Half",
      "task": [
        "The secret to sorting massive lists easily is to split them into tiny, manageable pieces. The first step of 'Merge Sort' is just chopping an array perfectly in half.",
        "",
        "Instructions:",
        "1. Define `halve_array(arr)`.",
        "2. Calculate the middle: `mid = len(arr) // 2`.",
        "3. Use the slice block from the 'Lists' tab. Make `left` equal to `arr` sliced from the beginning to `mid`.",
        "4. Make `right` equal to `arr` sliced from `mid` to the end.",
        "5. Return a list containing both: `[left, right]`.",
        "",
        "Sample Outputs:",
        "halve_array([1, 2, 3, 4]) → [[1, 2], [3, 4]]",
        "halve_array([5]) → [[], [5]]"
      ],
      "targetTime": "O(n)",
      "targetSpace": "O(n)",
      "testCasesPool": [
        { "call": "halve_array([1, 2, 3, 4])", "expected": "[[1, 2], [3, 4]]", "isHidden": false },
        { "call": "halve_array([10, 20, 30])", "expected": "[[10], [20, 30]]", "isHidden": false },
        { "call": "halve_array([5])", "expected": "[[], [5]]", "isHidden": false },
        { "call": "halve_array([])", "expected": "[[], []]", "isHidden": true },
        { "call": "halve_array([1, 2, 3, 4, 5])", "expected": "[[1, 2], [3, 4, 5]]", "isHidden": true }
      ]
    },
    {
      "id": "m3_l3_a2",
      "title": "Zipping Two Sorted Lists",
      "task": [
        "Once we have small pieces, we need to stitch them back together. If we have two lists that are *already sorted*, we can zip them together like a zipper on a jacket.",
        "",
        "Instructions:",
        "1. Define `merge(left, right)`. Initialize `result = []`, `i = 0`, and `j = 0`.",
        "2. Loop while `i < len(left)` and `j < len(right)`.",
        "3. If `left[i] < right[j]`, append `left[i]` to `result` and increment `i`. Else, append `right[j]` and increment `j`.",
        "4. After the loop, use list joining (`+`) to add any leftovers: `result + left[i:] + right[j:]`.",
        "5. Return the result.",
        "",
        "Sample Outputs:",
        "merge([1, 3], [2, 4]) → [1, 2, 3, 4]",
        "merge([10], []) → [10]"
      ],
      "targetTime": "O(n)",
      "targetSpace": "O(n)",
      "testCasesPool": [
        { "call": "merge([1, 3], [2, 4])", "expected": "[1, 2, 3, 4]", "isHidden": false },
        { "call": "merge([5, 9], [1, 8])", "expected": "[1, 5, 8, 9]", "isHidden": false },
        { "call": "merge([10], [])", "expected": "[10]", "isHidden": false },
        { "call": "merge([], [5])", "expected": "[5]", "isHidden": true },
        { "call": "merge([1, 1], [1, 1])", "expected": "[1, 1, 1, 1]", "isHidden": true }
      ]
    },
    {
      "id": "m3_l3_a3",
      "title": "Full Split and Stitch (Merge Sort)",
      "task": [
        "Now let's use recursion to put it all together! We will magically split the array down to single items, and then our `merge` function will zip them all back together in perfect order.",
        "",
        "Instructions:",
        "1. Define `merge_sort(arr)`. Ensure your `merge()` function from the last task is available.",
        "2. Base Case: If `len(arr) <= 1`, it's already sorted! Return `arr`.",
        "3. Calculate `mid = len(arr) // 2`.",
        "4. Split and recurse: `left = merge_sort(arr[:mid])` and `right = merge_sort(arr[mid:])`.",
        "5. Return the final zip: `merge(left, right)`.",
        "",
        "Sample Outputs:",
        "merge_sort([4, 2, 3, 1]) → [1, 2, 3, 4]",
        "merge_sort([5, 5, 5]) → [5, 5, 5]"
      ],
      "targetTime": "O(n log n)",
      "targetSpace": "O(n)",
      "testCasesPool": [
        { "call": "merge_sort([4, 2, 3, 1])", "expected": "[1, 2, 3, 4]", "isHidden": false },
        { "call": "merge_sort([38, 27, 43, 3, 9, 82, 10])", "expected": "[3, 9, 10, 27, 38, 43, 82]", "isHidden": false },
        { "call": "merge_sort([5, 5, 5])", "expected": "[5, 5, 5]", "isHidden": false },
        { "call": "merge_sort([])", "expected": "[]", "isHidden": true },
        { "call": "merge_sort([10, -2, 8])", "expected": "[-2, 8, 10]", "isHidden": true }
      ]
    },
    {
      "id": "m3_l3_a4",
      "title": "Merge Sort Backwards",
      "task": [
        "To invert the sorting order, the big recursive splitting stays exactly the same. The only thing we have to flip is the logic inside the zipping `merge` helper function.",
        "",
        "Instructions:",
        "1. Define `merge_sort_desc(arr)` with an inner `merge_desc(left, right)` helper function.",
        "2. Within the helper, flip the comparison: evaluate `if left[i] >= right[j]`.",
        "3. If true, append from `left` and advance `i`. Otherwise, append from `right` and advance `j`.",
        "4. Return the merged results exactly like before.",
        "",
        "Sample Outputs:",
        "merge_sort_desc([1, 2, 3]) → [3, 2, 1]",
        "merge_sort_desc([5, 1, 4]) → [5, 4, 1]"
      ],
      "targetTime": "O(n log n)",
      "targetSpace": "O(n)",
      "testCasesPool": [
        { "call": "merge_sort_desc([1, 2, 3])", "expected": "[3, 2, 1]", "isHidden": false },
        { "call": "merge_sort_desc([5, 1, 4])", "expected": "[5, 4, 1]", "isHidden": false },
        { "call": "merge_sort_desc([7, 7, 7])", "expected": "[7, 7, 7]", "isHidden": false },
        { "call": "merge_sort_desc([])", "expected": "[]", "isHidden": true },
        { "call": "merge_sort_desc([-5, -1, 0])", "expected": "[0, -1, -5]", "isHidden": true }
      ]
    },
    {
      "id": "m3_l3_a5",
      "title": "Sorting Words",
      "task": [
        "Merge Sort is brilliant because it works perfectly on text too! Python naturally knows how to alphabetize words using the exact same `<` and `>` operators.",
        "",
        "Instructions:",
        "1. Define `merge_strings(arr)`.",
        "2. Replicate the standard ascending `merge_sort` logic identically.",
        "3. Do not change a thing. Pass in an array of words and watch it alphabetize them instantly!",
        "",
        "Sample Outputs:",
        "merge_strings(['c', 'a', 'b']) → ['a', 'b', 'c']",
        "merge_strings(['apple', 'banana', 'ant']) → ['ant', 'apple', 'banana']"
      ],
      "targetTime": "O(n log n)",
      "targetSpace": "O(n)",
      "testCasesPool": [
        { "call": "merge_strings(['c', 'a', 'b'])", "expected": "['a', 'b', 'c']", "isHidden": false },
        { "call": "merge_strings(['z', 'x', 'y'])", "expected": "['x', 'y', 'z']", "isHidden": false },
        { "call": "merge_strings(['apple', 'banana', 'ant'])", "expected": "['ant', 'apple', 'banana']", "isHidden": false },
        { "call": "merge_strings([])", "expected": "[]", "isHidden": true },
        { "call": "merge_strings(['a'])", "expected": "['a']", "isHidden": true }
      ]
    }
  ],
  "lesson_4": [
    {
      "id": "m3_l4_a1",
      "title": "Picking a Leader (Partitioning)",
      "task": [
        "Quick Sort works by picking one number to be the 'Pivot' (the leader). Everyone smaller than the leader goes to the left room, and everyone bigger goes to the right room.",
        "",
        "Instructions:",
        "1. Define `partition(arr)`. If `arr` is empty, return `[]`.",
        "2. Set `pivot = arr[0]`. Make two empty lists: `left` and `right`.",
        "3. Loop through the rest of the list (`arr[1:]`).",
        "4. If a number is `< pivot`, append it to `left`. Otherwise, append it to `right`.",
        "5. Return a list containing three items exactly like this: `[left, pivot, right]`.",
        "",
        "Sample Outputs:",
        "partition([5, 1, 8, 2]) → [[1, 2], 5, [8]]",
        "partition([10, 20, 5]) → [[5], 10, [20]]"
      ],
      "targetTime": "O(n)",
      "targetSpace": "O(n)",
      "testCasesPool": [
        { "call": "partition([5, 1, 8, 2])", "expected": "[[1, 2], 5, [8]]", "isHidden": false },
        { "call": "partition([10, 20, 5])", "expected": "[[5], 10, [20]]", "isHidden": false },
        { "call": "partition([3, 3, 3])", "expected": "[[], 3, [3, 3]]", "isHidden": false },
        { "call": "partition([])", "expected": "[]", "isHidden": true },
        { "call": "partition([7])", "expected": "[[], 7, []]", "isHidden": true }
      ]
    },
    {
      "id": "m3_l4_a2",
      "title": "Full Quick Sort",
      "task": [
        "Now we use recursion to do that partitioning trick over and over. We sort the left room, we sort the right room, and then stick them together with the leader in the middle!",
        "",
        "Instructions:",
        "1. Define `quick_sort(arr)`.",
        "2. Base Case: If `len(arr) <= 1`, return `arr`.",
        "3. Set `pivot = arr[0]`. Split the rest into `left` and `right` arrays exactly like the last task.",
        "4. Return the joined lists: `quick_sort(left) + [pivot] + quick_sort(right)`.",
        "",
        "Sample Outputs:",
        "quick_sort([3, 2, 1, 4]) → [1, 2, 3, 4]",
        "quick_sort([5, 5, 5]) → [5, 5, 5]"
      ],
      "targetTime": "O(n log n)",
      "targetSpace": "O(n)",
      "testCasesPool": [
        { "call": "quick_sort([3, 2, 1, 4])", "expected": "[1, 2, 3, 4]", "isHidden": false },
        { "call": "quick_sort([10, 7, 8, 9, 1, 5])", "expected": "[1, 5, 7, 8, 9, 10]", "isHidden": false },
        { "call": "quick_sort([5, 5, 5])", "expected": "[5, 5, 5]", "isHidden": false },
        { "call": "quick_sort([])", "expected": "[]", "isHidden": true },
        { "call": "quick_sort([1])", "expected": "[1]", "isHidden": true }
      ]
    },
    {
      "id": "m3_l4_a3",
      "title": "Quick Sort Backwards",
      "task": [
        "To sort in descending order using Quick Sort, we just flip the door signs! Put the *bigger* numbers in the left room, and the *smaller* numbers in the right room.",
        "",
        "Instructions:",
        "1. Define `quick_sort_desc(arr)`.",
        "2. Handle the base case: if `len(arr) <= 1`, return `arr`.",
        "3. Extract `pivot = arr[0]`. Iterate through `arr[1:]`.",
        "4. If a number is `> pivot`, append it to the `left` array. Otherwise, append to `right`.",
        "5. Return `quick_sort_desc(left) + [pivot] + quick_sort_desc(right)`.",
        "",
        "Sample Outputs:",
        "quick_sort_desc([1, 2, 3]) → [3, 2, 1]",
        "quick_sort_desc([5, 1, 4, 8]) → [8, 5, 4, 1]"
      ],
      "targetTime": "O(n log n)",
      "targetSpace": "O(n)",
      "testCasesPool": [
        { "call": "quick_sort_desc([1, 2, 3])", "expected": "[3, 2, 1]", "isHidden": false },
        { "call": "quick_sort_desc([5, 1, 4, 8])", "expected": "[8, 5, 4, 1]", "isHidden": false },
        { "call": "quick_sort_desc([7, 7, 7])", "expected": "[7, 7, 7]", "isHidden": false },
        { "call": "quick_sort_desc([])", "expected": "[]", "isHidden": true },
        { "call": "quick_sort_desc([-5, 0, 5])", "expected": "[5, 0, -5]", "isHidden": true }
      ]
    },
    {
      "id": "m3_l4_a4",
      "title": "Sorting Words Quickly",
      "task": [
        "Just like Merge Sort, Quick Sort is brilliant at alphabetizing strings.",
        "",
        "Instructions:",
        "1. Define `quick_sort_strings(arr)`.",
        "2. Implement the standard ascending `quick_sort` architecture you built in Activity 2.",
        "3. Do not change anything! Python's operators will automatically send 'A' words to the left and 'Z' words to the right.",
        "",
        "Sample Outputs:",
        "quick_sort_strings(['delta', 'alpha', 'charlie']) → ['alpha', 'charlie', 'delta']",
        "quick_sort_strings(['same', 'same']) → ['same', 'same']"
      ],
      "targetTime": "O(n log n)",
      "targetSpace": "O(n)",
      "testCasesPool": [
        { "call": "quick_sort_strings(['delta', 'alpha', 'charlie'])", "expected": "['alpha', 'charlie', 'delta']", "isHidden": false },
        { "call": "quick_sort_strings(['z', 'a', 'm'])", "expected": "['a', 'm', 'z']", "isHidden": false },
        { "call": "quick_sort_strings(['same', 'same'])", "expected": "['same', 'same']", "isHidden": false },
        { "call": "quick_sort_strings([])", "expected": "[]", "isHidden": true },
        { "call": "quick_sort_strings(['b'])", "expected": "['b']", "isHidden": true }
      ]
    },
    {
      "id": "m3_l4_a5",
      "title": "Tracking the Pivots",
      "task": [
        "To really understand how Quick Sort breaks things down, let's strip away the sorting entirely and write a function that just spits out a list of every leader (pivot) it picks along the way.",
        "",
        "Instructions:",
        "1. Define `pivot_track(arr)`.",
        "2. Base Case: If `len(arr) <= 1`, return `[]` if empty, or `[arr[0]]` if not empty.",
        "3. Extract `pivot = arr[0]`. Generate the `left` and `right` partitions normally.",
        "4. Return exactly this: `pivot_track(left) + [pivot] + pivot_track(right)`. This returns only the sequence of pivots, not the fully sorted array!",
        "",
        "Sample Outputs:",
        "pivot_track([3, 1, 4]) → [1, 3, 4]",
        "pivot_track([5, 2, 8]) → [2, 5, 8]"
      ],
      "targetTime": "O(n log n)",
      "targetSpace": "O(n)",
      "testCasesPool": [
        { "call": "pivot_track([3, 1, 4])", "expected": "[1, 3, 4]", "isHidden": false },
        { "call": "pivot_track([5, 2, 8])", "expected": "[2, 5, 8]", "isHidden": false },
        { "call": "pivot_track([10, 20, 30])", "expected": "[10, 20, 30]", "isHidden": false },
        { "call": "pivot_track([])", "expected": "[]", "isHidden": true },
        { "call": "pivot_track([7])", "expected": "[7]", "isHidden": true }
      ]
    }
  ],
  "optimizations": [
    {
      "id": "m3_opt_1",
      "title": "Optimization Challenge: Slow to Fast Search",
      "task": [
        "A linear scan (O(n)) through a sorted list is highly inefficient for large datasets. Refactoring the traversal into a Binary Search structure reduces the time it takes to a rapid O(log n).",
        "",
        "Optimization Guidelines:",
        "1. Analyze the current implementation: the `for` loop checks every single item.",
        "2. Delete the `for` loop entirely.",
        "3. Introduce `low` and `high` pointer variables to establish your search boundaries.",
        "4. Implement a `while` loop that calculates the middle. Adjust the boundaries to systematically halve the search space.",
        "5. Observe the analytical metrics demonstrating the transition to O(log n) efficiency.",
        "",
        "Sample Outputs:",
        "search([1, 3, 5, 7, 9], 7) → 3",
        "search([10, 20, 30, 40], 10) → 0"
      ],
      "targetTime": "O(log n)",
      "targetSpace": "O(1)",
      "templateUrl": "/data/optimizations/m3-3.1.json",
      "testCasesPool": [
        { "call": "search([1, 3, 5, 7, 9], 7)", "expected": "3", "isHidden": false },
        { "call": "search([10, 20, 30, 40], 10)", "expected": "0", "isHidden": false },
        { "call": "search([2, 4, 6], 5)", "expected": "-1", "isHidden": false },
        { "call": "search([], 1)", "expected": "-1", "isHidden": true },
        { "call": "search([5], 5)", "expected": "0", "isHidden": true }
      ]
    },
    {
      "id": "m3_opt_2",
      "title": "Optimization Challenge: Finding the Peak",
      "task": [
        "A peak element is an integer strictly greater than its neighbors. Instead of an O(n) sequential scan, we can deploy a modified binary search to intuitively follow the slope uphill in O(log n) time.",
        "",
        "Optimization Guidelines:",
        "1. Eliminate the inefficient O(n) sequential loop.",
        "2. Establish `low` and `high` pointer variables.",
        "3. Within a `while low < high` loop, compute the midpoint index.",
        "4. Evaluate the slope: if `arr[mid] < arr[mid + 1]`, the slope is going UP to the right. Adjust the `low` pointer up to `mid + 1`.",
        "5. If the slope is going DOWN, the peak is in the left half. Reduce the `high` pointer to `mid`.",
        "6. Return the `low` pointer index when the loop concludes.",
        "",
        "Sample Outputs:",
        "find_peak([1, 2, 3, 1]) → 2",
        "find_peak([1, 2, 1, 3, 5, 6, 4]) → 5"
      ],
      "targetTime": "O(log n)",
      "targetSpace": "O(1)",
      "templateUrl": "/data/optimizations/m3-3.2.json",
      "testCasesPool": [
        { "call": "find_peak([1, 2, 3, 1])", "expected": "2", "isHidden": false },
        { "call": "find_peak([1, 2, 1, 3, 5, 6, 4])", "expected": "5", "isHidden": false },
        { "call": "find_peak([1, 2, 3, 4, 5])", "expected": "4", "isHidden": false },
        { "call": "find_peak([10, 9, 8, 7])", "expected": "0", "isHidden": true },
        { "call": "find_peak([5])", "expected": "0", "isHidden": true }
      ]
    },
    {
      "id": "m3_opt_3",
      "title": "Optimization Challenge: Avoiding the Call Stack",
      "task": [
        "When you run a recursive Binary Search, you leave a trail of memory behind for every function call. Converting the recursion into a standard `while` loop optimizes this to O(1) space.",
        "",
        "Optimization Guidelines:",
        "1. Identify the memory stack generated by returning the function within itself (e.g., `return search_optimized(...)`).",
        "2. Remove all recursive function calls from the structure.",
        "3. Refactor the core logic using an iterative `while low <= high:` loop structure.",
        "4. Reassign the `low` and `high` pointer variables directly within the loop instead of passing them as parameters to a new function.",
        "5. Verify through the analyzer that the Space Complexity has successfully dropped to O(1).",
        "",
        "Sample Outputs:",
        "search_optimized([10, 20, 30, 40, 50], 40) → 3",
        "search_optimized([1, 2, 3], 2) → 1"
      ],
      "targetTime": "O(log n)",
      "targetSpace": "O(1)",
      "templateUrl": "/data/optimizations/m3-3.3.json",
      "testCasesPool": [
        { "call": "search_optimized([10, 20, 30, 40, 50], 40)", "expected": "3", "isHidden": false },
        { "call": "search_optimized([1, 2, 3], 2)", "expected": "1", "isHidden": false },
        { "call": "search_optimized([5, 8, 12], 20)", "expected": "-1", "isHidden": false },
        { "call": "search_optimized([], 5)", "expected": "-1", "isHidden": true },
        { "call": "search_optimized([7], 7)", "expected": "0", "isHidden": true }
      ]
    }
  ]
}