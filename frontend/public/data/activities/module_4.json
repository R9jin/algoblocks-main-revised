{
  "lesson_1": [
    {
      "id": "m4_l1_a1",
      "title": "Adding to a Stack (Push)",
      "task": [
        "A 'Stack' works exactly like a stack of plates in a cafeteria. You can only add new plates to the very top! In programming, adding to the top of a stack is called a 'Push'.",
        "",
        "Instructions:",
        "1. Define the function `push_stack(stack, item)`.",
        "2. Open the 'Stacks & Queues (Abstract Data Types)' tab.",
        "3. Grab the 'push [item] to stack [stack]' block.",
        "4. Put your `item` variable in the first slot, and your `stack` variable in the second slot.",
        "5. Return the updated `stack`.",
        "",
        "Sample Outputs:",
        "push_stack([10, 20], 30) → [10, 20, 30]",
        "push_stack([], 5) → [5]"
      ],
      "targetTime": "O(1)",
      "targetSpace": "O(1)",
      "testCasesPool": [
        { "call": "push_stack([10, 20], 30)", "expected": "[10, 20, 30]", "isHidden": false },
        { "call": "push_stack([], 5)", "expected": "[5]", "isHidden": false },
        { "call": "push_stack([1], 1)", "expected": "[1, 1]", "isHidden": false },
        { "call": "push_stack([-5, 0], 5)", "expected": "[-5, 0, 5]", "isHidden": true },
        { "call": "push_stack([99], 100)", "expected": "[99, 100]", "isHidden": true }
      ]
    },
    {
      "id": "m4_l1_a2",
      "title": "Removing from a Stack (Pop)",
      "task": [
        "To get a plate from our stack, we have to take the one on the very top. We can't pull from the bottom! Taking the top item off is called a 'Pop'.",
        "",
        "Instructions:",
        "1. Define the function `pop_stack(stack)`.",
        "2. Use an 'If' block from the 'Logic' tab to check if the stack is empty. If it is, return the text `'Underflow'` (which means we tried to grab a plate that isn't there!).",
        "3. Open the 'Stacks & Queues' tab and use the 'pop and get top of stack' block.",
        "4. Return the item that you just popped.",
        "",
        "Sample Outputs:",
        "pop_stack([10, 20, 30]) → 30",
        "pop_stack([]) → 'Underflow'"
      ],
      "targetTime": "O(1)",
      "targetSpace": "O(1)",
      "testCasesPool": [
        { "call": "pop_stack([10, 20, 30])", "expected": "30", "isHidden": false },
        { "call": "pop_stack([5])", "expected": "5", "isHidden": false },
        { "call": "pop_stack([])", "expected": "'Underflow'", "isHidden": false },
        { "call": "pop_stack([1, 2])", "expected": "2", "isHidden": true },
        { "call": "pop_stack([-10, -20])", "expected": "-20", "isHidden": true }
      ]
    },
    {
      "id": "m4_l1_a3",
      "title": "Flipping a Word Backwards",
      "task": [
        "Because a stack gives you items back in the exact opposite order you put them in, they are the perfect tool for reading words backwards!",
        "",
        "Instructions:",
        "1. Define `reverse_string(s)`.",
        "2. Create an empty list called `stack` and an empty text variable called `result`.",
        "3. Use a 'for each item' loop to look at every letter in the string `s`. Push each letter onto your `stack`.",
        "4. Make a `while` loop that runs as long as the stack is NOT empty.",
        "5. Inside the while loop, pop a letter off the stack and join it to your `result` variable.",
        "6. Return the `result`.",
        "",
        "Sample Outputs:",
        "reverse_string('data') → 'atad'",
        "reverse_string('stack') → 'kcats'"
      ],
      "targetTime": "O(n)",
      "targetSpace": "O(n)",
      "testCasesPool": [
        { "call": "reverse_string('data')", "expected": "'atad'", "isHidden": false },
        { "call": "reverse_string('stack')", "expected": "'kcats'", "isHidden": false },
        { "call": "reverse_string('a')", "expected": "'a'", "isHidden": false },
        { "call": "reverse_string('')", "expected": "''", "isHidden": true },
        { "call": "reverse_string('12345')", "expected": "'54321'", "isHidden": true }
      ]
    },
    {
      "id": "m4_l1_a4",
      "title": "Matching Parentheses",
      "task": [
        "Computers use stacks to make sure every open parenthesis '(' has a matching closed parenthesis ')'.",
        "",
        "Instructions:",
        "1. Define `is_balanced(s)` and initialize an empty `stack`.",
        "2. Loop through each `char` in the string `s`.",
        "3. If `char == '('`, push it onto the stack.",
        "4. If `char == ')'`, first check if the stack is empty (if it is, return `False` because there's no matching opener!). If it's not empty, pop the top item off the stack.",
        "5. After the loop finishes, check if the stack is perfectly empty. If it is empty, return `True`. Otherwise, return `False`.",
        "",
        "Sample Outputs:",
        "is_balanced('(())') → True",
        "is_balanced('(()') → False"
      ],
      "targetTime": "O(n)",
      "targetSpace": "O(n)",
      "testCasesPool": [
        { "call": "is_balanced('(())')", "expected": "True", "isHidden": false },
        { "call": "is_balanced('()()')", "expected": "True", "isHidden": false },
        { "call": "is_balanced('(()')", "expected": "False", "isHidden": false },
        { "call": "is_balanced(')')", "expected": "False", "isHidden": true },
        { "call": "is_balanced('')", "expected": "True", "isHidden": true }
      ]
    },
    {
      "id": "m4_l1_a5",
      "title": "Stack Calculator",
      "task": [
        "Let's build a mini calculator using a stack! When we see a number, we push it to the stack. When we see a math symbol, we pop two numbers, do the math, and put the answer back on the stack.",
        "",
        "Instructions:",
        "1. Define `evaluate_postfix(arr)` and create an empty `stack`.",
        "2. Loop over every `item` in `arr`. Open the 'Logic' tab and use the 'is [item] a [int]?' block.",
        "3. If it is an integer (number), push it to the stack.",
        "4. If it's not a number, it's an operator! Pop the stack and save it as `b`. Pop the stack again and save it as `a`. Calculate `a (operator) b` based on the item ('+' or '*' or '-') and push the answer back.",
        "5. Return the final answer by popping the stack one last time.",
        "",
        "Sample Outputs:",
        "evaluate_postfix([3, 4, '+', 2, '*']) → 14",
        "evaluate_postfix([10, 2, '*']) → 20"
      ],
      "targetTime": "O(n)",
      "targetSpace": "O(n)",
      "testCasesPool": [
        { "call": "evaluate_postfix([3, 4, '+', 2, '*'])", "expected": "14", "isHidden": false },
        { "call": "evaluate_postfix([5, 1, 2, '+', 4, '*', '+', 3, '-'])", "expected": "14", "isHidden": false },
        { "call": "evaluate_postfix([10, 2, '*'])", "expected": "20", "isHidden": false },
        { "call": "evaluate_postfix([8, 2, '-'])", "expected": "6", "isHidden": true },
        { "call": "evaluate_postfix([7])", "expected": "7", "isHidden": true }
      ]
    }
  ],
  "lesson_2": [
    {
      "id": "m4_l2_a1",
      "title": "Waiting in Line (Enqueue)",
      "task": [
        "A 'Queue' works exactly like waiting in line at a movie theater. The first person in line is the first person to get a ticket! Adding someone to the back of the line is called 'Enqueuing'.",
        "",
        "Instructions:",
        "1. Define the function `enqueue(queue, item)`.",
        "2. Open the 'Stacks & Queues (Abstract Data Types)' tab.",
        "3. Grab the 'enqueue [item] to queue [queue]' block.",
        "4. Return the updated `queue` list.",
        "",
        "Sample Outputs:",
        "enqueue([1, 2], 3) → [1, 2, 3]",
        "enqueue([], 10) → [10]"
      ],
      "targetTime": "O(1)",
      "targetSpace": "O(1)",
      "testCasesPool": [
        { "call": "enqueue([1, 2], 3)", "expected": "[1, 2, 3]", "isHidden": false },
        { "call": "enqueue([], 10)", "expected": "[10]", "isHidden": false },
        { "call": "enqueue([5], 5)", "expected": "[5, 5]", "isHidden": false },
        { "call": "enqueue([-1], 0)", "expected": "[-1, 0]", "isHidden": true },
        { "call": "enqueue([100, 200], 300)", "expected": "[100, 200, 300]", "isHidden": true }
      ]
    },
    {
      "id": "m4_l2_a2",
      "title": "Leaving the Line (Dequeue)",
      "task": [
        "When the person at the front of the line is finished, they leave the queue. We call this 'Dequeuing'. Remember, we always remove from the front, never the back!",
        "",
        "Instructions:",
        "1. Define the function `dequeue(queue)`.",
        "2. Use an 'If' block to check if the queue is empty. If it is, return the text `'Underflow'`.",
        "3. Open the 'Stacks & Queues' tab and grab the 'dequeue and get front of queue' block.",
        "4. Return the item you just removed.",
        "",
        "Sample Outputs:",
        "dequeue([10, 20, 30]) → 10",
        "dequeue([]) → 'Underflow'"
      ],
      "targetTime": "O(n)",
      "targetSpace": "O(1)",
      "testCasesPool": [
        { "call": "dequeue([10, 20, 30])", "expected": "10", "isHidden": false },
        { "call": "dequeue([5])", "expected": "5", "isHidden": false },
        { "call": "dequeue([])", "expected": "'Underflow'", "isHidden": false },
        { "call": "dequeue([1, 2])", "expected": "1", "isHidden": true },
        { "call": "dequeue([-10, -20])", "expected": "-10", "isHidden": true }
      ]
    },
    {
      "id": "m4_l2_a3",
      "title": "Looking at the Front (Peek)",
      "task": [
        "Sometimes you just want to look at who is next in line without actually removing them. We call this a 'Peek'.",
        "",
        "Instructions:",
        "1. Define the function `peek_queue(queue)`.",
        "2. If the queue is empty, return the text `'Empty'`.",
        "3. Grab the 'peek front of queue' block from the 'Stacks & Queues' tab.",
        "4. Return the item you peeked at.",
        "",
        "Sample Outputs:",
        "peek_queue([5, 10, 15]) → 5",
        "peek_queue([]) → 'Empty'"
      ],
      "targetTime": "O(1)",
      "targetSpace": "O(1)",
      "testCasesPool": [
        { "call": "peek_queue([5, 10, 15])", "expected": "5", "isHidden": false },
        { "call": "peek_queue([99])", "expected": "99", "isHidden": false },
        { "call": "peek_queue([])", "expected": "'Empty'", "isHidden": false },
        { "call": "peek_queue([1, 2, 3])", "expected": "1", "isHidden": true },
        { "call": "peek_queue([-5, 0, 5])", "expected": "-5", "isHidden": true }
      ]
    },
    {
      "id": "m4_l2_a4",
      "title": "Generating a Pattern",
      "task": [
        "Queues are amazing at generating sequences in perfect order. Let's use a queue to generate binary numbers (1, 10, 11, 100).",
        "",
        "Instructions:",
        "1. Define `generate_binary(n)`.",
        "2. Start a `queue` list holding just the text `'1'`, and an empty `result` list.",
        "3. Loop exactly `n` times.",
        "4. Inside the loop, dequeue the front of the queue and save it as `curr`. Append `curr` to your `result` list.",
        "5. Next, enqueue `curr + '0'` to the queue, and then enqueue `curr + '1'` to the queue.",
        "6. Return the `result` list.",
        "",
        "Sample Outputs:",
        "generate_binary(3) → ['1', '10', '11']",
        "generate_binary(5) → ['1', '10', '11', '100', '101']"
      ],
      "targetTime": "O(n)",
      "targetSpace": "O(n)",
      "testCasesPool": [
        { "call": "generate_binary(3)", "expected": "['1', '10', '11']", "isHidden": false },
        { "call": "generate_binary(5)", "expected": "['1', '10', '11', '100', '101']", "isHidden": false },
        { "call": "generate_binary(1)", "expected": "['1']", "isHidden": false },
        { "call": "generate_binary(0)", "expected": "[]", "isHidden": true },
        { "call": "generate_binary(2)", "expected": "['1', '10']", "isHidden": true }
      ]
    },
    {
      "id": "m4_l2_a5",
      "title": "Reverse the Front of the Line",
      "task": [
        "Let's try a tricky maneuver: take the first 'K' people in the queue, reverse their order, and put them right back where they were!",
        "",
        "Instructions:",
        "1. Define `reverse_k(queue, k)`. If `k` is 0 or bigger than the queue length, just return the `queue`.",
        "2. Slice the queue from the beginning to index `k` to get the first part. Use the 'reverse list' block to flip it backwards.",
        "3. Slice the queue from index `k` to the end to get the remaining part.",
        "4. Join your flipped first part with your untouched remaining part using the 'join list' block.",
        "5. Return the combined list.",
        "",
        "Sample Outputs:",
        "reverse_k([1, 2, 3, 4, 5], 3) → [3, 2, 1, 4, 5]",
        "reverse_k([10, 20, 30], 2) → [20, 10, 30]"
      ],
      "targetTime": "O(n)",
      "targetSpace": "O(k)",
      "testCasesPool": [
        { "call": "reverse_k([1, 2, 3, 4, 5], 3)", "expected": "[3, 2, 1, 4, 5]", "isHidden": false },
        { "call": "reverse_k([10, 20, 30], 2)", "expected": "[20, 10, 30]", "isHidden": false },
        { "call": "reverse_k([1, 2], 5)", "expected": "[1, 2]", "isHidden": false },
        { "call": "reverse_k([1, 2, 3], 0)", "expected": "[1, 2, 3]", "isHidden": true },
        { "call": "reverse_k([7, 8, 9, 10], 4)", "expected": "[10, 9, 8, 7]", "isHidden": true }
      ]
    }
  ],
  "lesson_3": [
    {
      "id": "m4_l3_a1",
      "title": "Keeping a Tally (Dictionaries)",
      "task": [
        "A 'Dictionary' (or Hash Map) is like a tally sheet. You give it a name (the Key), and it stores a piece of data for you (the Value). Let's use one to count how many times numbers appear in a list.",
        "",
        "Instructions:",
        "1. Define the function `frequency_map(arr)`.",
        "2. Open the 'Dictionaries (Built-in Type)' tab and create an empty dictionary called `tally`.",
        "3. Loop through every `item` in `arr`. Use an 'If/Else' block.",
        "4. If `item` is already in `tally` (use 'is in' from Logic), set its dictionary key to its current value + 1.",
        "5. Else (it's brand new), set its dictionary key to 1.",
        "6. Return the `tally` dictionary.",
        "",
        "Sample Outputs:",
        "frequency_map([1, 2, 2, 3]) → {1: 1, 2: 2, 3: 1}",
        "frequency_map([5, 5, 5]) → {5: 3}"
      ],
      "targetTime": "O(n)",
      "targetSpace": "O(n)",
      "testCasesPool": [
        { "call": "frequency_map([1, 2, 2, 3])", "expected": "{1: 1, 2: 2, 3: 1}", "isHidden": false },
        { "call": "frequency_map([5, 5, 5])", "expected": "{5: 3}", "isHidden": false },
        { "call": "frequency_map([])", "expected": "{}", "isHidden": false },
        { "call": "frequency_map([10])", "expected": "{10: 1}", "isHidden": true },
        { "call": "frequency_map([1, 2, 1, 2])", "expected": "{1: 2, 2: 2}", "isHidden": true }
      ]
    },
    {
      "id": "m4_l3_a2",
      "title": "Finding the First Unique Letter",
      "task": [
        "Let's find the very first letter in a word that doesn't repeat! We can use our dictionary tally sheet to help us.",
        "",
        "Instructions:",
        "1. Define `first_unique(s)`.",
        "2. Use the exact same logic from the previous task to build a tally dictionary of all the letters in the string `s`.",
        "3. Once your tally is built, make a SECOND loop that walks through the string `s` again.",
        "4. For each letter, check your dictionary. If the tally for that letter is exactly `1`, return the letter immediately!",
        "5. If you check the whole word and everything repeats, return `'_'`.",
        "",
        "Sample Outputs:",
        "first_unique('leetcode') → 'l'",
        "first_unique('aabb') → '_'"
      ],
      "targetTime": "O(n)",
      "targetSpace": "O(k)",
      "testCasesPool": [
        { "call": "first_unique('leetcode')", "expected": "'l'", "isHidden": false },
        { "call": "first_unique('aabb')", "expected": "'_'", "isHidden": false },
        { "call": "first_unique('z')", "expected": "'z'", "isHidden": false },
        { "call": "first_unique('aabbc')", "expected": "'c'", "isHidden": true },
        { "call": "first_unique('')", "expected": "'_'", "isHidden": true }
      ]
    },
    {
      "id": "m4_l3_a3",
      "title": "Finding Matches (Intersection)",
      "task": [
        "To find items that live in both lists, checking them one by one is too slow. If we turn one list into a 'Set', the computer can check it instantly!",
        "",
        "Instructions:",
        "1. Define `intersect_arrays(arr1, arr2)`.",
        "2. Open the 'Sets & Tuples' tab. Turn `arr1` into a Set and save it as `set1`. Create an empty list called `result`.",
        "3. Loop through `arr2`. Use an 'If' block: if the item is in `set1`, append it to `result`.",
        "4. CRITICAL: Once you append it, use the 'remove from set' block to delete it from `set1`. This prevents duplicate answers!",
        "5. Return `result`.",
        "",
        "Sample Outputs:",
        "intersect_arrays([1, 2, 2, 1], [2, 2]) → [2]",
        "intersect_arrays([4, 9, 5], [9, 4, 9, 8, 4]) → [9, 4]"
      ],
      "targetTime": "O(n + m)",
      "targetSpace": "O(n)",
      "testCasesPool": [
        { "call": "intersect_arrays([1, 2, 2, 1], [2, 2])", "expected": "[2]", "isHidden": false },
        { "call": "intersect_arrays([4, 9, 5], [9, 4, 9, 8, 4])", "expected": "[9, 4]", "isHidden": false },
        { "call": "intersect_arrays([1, 2], [3, 4])", "expected": "[]", "isHidden": false },
        { "call": "intersect_arrays([], [1])", "expected": "[]", "isHidden": true },
        { "call": "intersect_arrays([1, 1], [1, 1])", "expected": "[1]", "isHidden": true }
      ]
    },
    {
      "id": "m4_l3_a4",
      "title": "Are They Anagrams?",
      "task": [
        "Two words are anagrams if they use the exact same letters (like 'rat' and 'tar'). Let's use a dictionary to check!",
        "",
        "Instructions:",
        "1. Define `is_anagram(s, t)`.",
        "2. First check: if the length of `s` does not equal the length of `t`, return `False`.",
        "3. Build a frequency tally dictionary for string `s`.",
        "4. Loop through string `t`. For each letter, subtract 1 from its dictionary value. If a letter isn't in the dictionary at all, return `False`.",
        "5. After the loop, check the dictionary values (using 'get values from dict'). If any value is not exactly 0, return `False`. Otherwise return `True`.",
        "",
        "Sample Outputs:",
        "is_anagram('anagram', 'nagaram') → True",
        "is_anagram('rat', 'car') → False"
      ],
      "targetTime": "O(n)",
      "targetSpace": "O(k)",
      "testCasesPool": [
        { "call": "is_anagram('anagram', 'nagaram')", "expected": "True", "isHidden": false },
        { "call": "is_anagram('rat', 'car')", "expected": "False", "isHidden": false },
        { "call": "is_anagram('a', 'a')", "expected": "True", "isHidden": false },
        { "call": "is_anagram('ab', 'a')", "expected": "False", "isHidden": true },
        { "call": "is_anagram('', '')", "expected": "True", "isHidden": true }
      ]
    },
    {
      "id": "m4_l3_a5",
      "title": "Secret Codes",
      "task": [
        "Can one word be turned into another by replacing letters? 'egg' can become 'add' if 'e'='a' and 'g'='d'. But 'foo' can't be 'bar' because 'o' can't be both 'a' and 'r'!",
        "",
        "Instructions:",
        "1. Define `is_isomorphic(s, t)`.",
        "2. Create two empty dictionaries: `map_s` and `map_t`.",
        "3. Use a 'count with i' loop to look at both words at the same time. Save `char_s = s[i]` and `char_t = t[i]`.",
        "4. If `char_s` is in `map_s` but its saved value isn't `char_t`, return `False`.",
        "5. If `char_t` is in `map_t` but its saved value isn't `char_s`, return `False`.",
        "6. Set `map_s[char_s] = char_t` and `map_t[char_t] = char_s`. Return `True` at the end.",
        "",
        "Sample Outputs:",
        "is_isomorphic('egg', 'add') → True",
        "is_isomorphic('foo', 'bar') → False"
      ],
      "targetTime": "O(n)",
      "targetSpace": "O(n)",
      "testCasesPool": [
        { "call": "is_isomorphic('egg', 'add')", "expected": "True", "isHidden": false },
        { "call": "is_isomorphic('foo', 'bar')", "expected": "False", "isHidden": false },
        { "call": "is_isomorphic('paper', 'title')", "expected": "True", "isHidden": false },
        { "call": "is_isomorphic('a', 'a')", "expected": "True", "isHidden": true },
        { "call": "is_isomorphic('ab', 'aa')", "expected": "False", "isHidden": true }
      ]
    }
  ],
  "lesson_4": [
    {
      "id": "m4_l4_a1",
      "title": "The Magic of Sets",
      "task": [
        "A 'Set' is a magical box that destroys duplicates instantly. You can never have two of the same item inside a Set!",
        "",
        "Instructions:",
        "1. Define the function `filter_unique(arr)`.",
        "2. Open the 'Sets & Tuples' tab and use 'create set from list' on your `arr`.",
        "3. Now open the 'Lists (Built-in Type)' tab and turn the Set right back into a list.",
        "4. Use the 'get sorted copy of list' block to sort it in ascending order, and return it.",
        "",
        "Sample Outputs:",
        "filter_unique([3, 1, 2, 1, 3]) → [1, 2, 3]",
        "filter_unique([5, 5, 5]) → [5]"
      ],
      "targetTime": "O(n log n)",
      "targetSpace": "O(n)",
      "testCasesPool": [
        { "call": "filter_unique([3, 1, 2, 1, 3])", "expected": "[1, 2, 3]", "isHidden": false },
        { "call": "filter_unique([5, 5, 5])", "expected": "[5]", "isHidden": false },
        { "call": "filter_unique([10, 20])", "expected": "[10, 20]", "isHidden": false },
        { "call": "filter_unique([])", "expected": "[]", "isHidden": true },
        { "call": "filter_unique([-1, -1, 0, 1])", "expected": "[-1, 0, 1]", "isHidden": true }
      ]
    },
    {
      "id": "m4_l4_a2",
      "title": "Is it a Subset?",
      "task": [
        "If you want to know if every single item from a small list exists inside a big list, Sets have a built-in tool that does this instantly without any loops!",
        "",
        "Instructions:",
        "1. Define `is_subset(sub_arr, super_arr)`.",
        "2. Turn both `sub_arr` and `super_arr` into Sets.",
        "3. You can use Python's built in math operators for this: evaluate if `set1 <= set2` using a logic block.",
        "4. Return the result.",
        "",
        "Sample Outputs:",
        "is_subset([1, 2], [1, 2, 3, 4]) → True",
        "is_subset([5], [1, 2, 3]) → False"
      ],
      "targetTime": "O(n + m)",
      "targetSpace": "O(n + m)",
      "testCasesPool": [
        { "call": "is_subset([1, 2], [1, 2, 3, 4])", "expected": "True", "isHidden": false },
        { "call": "is_subset([5], [1, 2, 3])", "expected": "False", "isHidden": false },
        { "call": "is_subset([], [1])", "expected": "True", "isHidden": false },
        { "call": "is_subset([10, 10], [10])", "expected": "True", "isHidden": true },
        { "call": "is_subset([1, 2, 3], [1, 2, 3])", "expected": "True", "isHidden": true }
      ]
    },
    {
      "id": "m4_l4_a3",
      "title": "Completely Different (Disjoint)",
      "task": [
        "Two lists are 'disjoint' if they share zero items. It means they are completely different. Let's use Sets to find out!",
        "",
        "Instructions:",
        "1. Define the function `is_disjoint(arr1, arr2)`.",
        "2. Turn both inputs into Sets.",
        "3. Use the 'set intersection' block from the 'Sets & Tuples' tab. This creates a new set of the items they share.",
        "4. Check the length of this intersection. If the length is 0, return `True` (they are totally different!). Otherwise, return `False`.",
        "",
        "Sample Outputs:",
        "is_disjoint([1, 2], [3, 4]) → True",
        "is_disjoint([1, 2, 3], [3, 4]) → False"
      ],
      "targetTime": "O(n + m)",
      "targetSpace": "O(n + m)",
      "testCasesPool": [
        { "call": "is_disjoint([1, 2], [3, 4])", "expected": "True", "isHidden": false },
        { "call": "is_disjoint([1, 2, 3], [3, 4])", "expected": "False", "isHidden": false },
        { "call": "is_disjoint([], [1, 2])", "expected": "True", "isHidden": false },
        { "call": "is_disjoint([5], [5])", "expected": "False", "isHidden": true },
        { "call": "is_disjoint([10, 20], [30, 40, 50])", "expected": "True", "isHidden": true }
      ]
    },
    {
      "id": "m4_l4_a4",
      "title": "Find the Missing Number",
      "task": [
        "Here's a famous math trick! If you have a list of numbers from 0 to N, but exactly one is missing, you can find it instantly by subtracting the actual sum from the expected sum.",
        "",
        "Instructions:",
        "1. Define the function `find_missing(arr)`.",
        "2. Save the length of the array to a variable `n`.",
        "3. Calculate what the sum SHOULD be using the formula: `(n * (n + 1)) // 2`. Save this as `expected_sum`.",
        "4. Add up all the numbers actually in `arr` using a loop, and save it as `actual_sum`.",
        "5. Return `expected_sum - actual_sum`.",
        "",
        "Sample Outputs:",
        "find_missing([3, 0, 1]) → 2",
        "find_missing([0, 1]) → 2"
      ],
      "targetTime": "O(n)",
      "targetSpace": "O(1)",
      "testCasesPool": [
        { "call": "find_missing([3, 0, 1])", "expected": "2", "isHidden": false },
        { "call": "find_missing([0, 1])", "expected": "2", "isHidden": false },
        { "call": "find_missing([9, 6, 4, 2, 3, 5, 7, 0, 1])", "expected": "8", "isHidden": false },
        { "call": "find_missing([0])", "expected": "1", "isHidden": true },
        { "call": "find_missing([1])", "expected": "0", "isHidden": true }
      ]
    },
    {
      "id": "m4_l4_a5",
      "title": "Check for Duplicates",
      "task": [
        "If you want to know if an array has duplicates, don't write a slow nested loop! Just use a Set and check the lengths.",
        "",
        "Instructions:",
        "1. Define `contains_duplicate(arr)`.",
        "2. Get the length of the original `arr`.",
        "3. Turn `arr` into a Set, and get the length of the Set.",
        "4. Use a Logic block: if the lengths are NOT equal, return `True` (because the Set threw away the duplicates!).",
        "5. If they are equal, return `False`.",
        "",
        "Sample Outputs:",
        "contains_duplicate([1, 2, 3, 1]) → True",
        "contains_duplicate([1, 2, 3, 4]) → False"
      ],
      "targetTime": "O(n)",
      "targetSpace": "O(n)",
      "testCasesPool": [
        { "call": "contains_duplicate([1, 2, 3, 1])", "expected": "True", "isHidden": false },
        { "call": "contains_duplicate([1, 2, 3, 4])", "expected": "False", "isHidden": false },
        { "call": "contains_duplicate([1, 1, 1, 3, 3, 4, 3, 2, 4, 2])", "expected": "True", "isHidden": false },
        { "call": "contains_duplicate([])", "expected": "False", "isHidden": true },
        { "call": "contains_duplicate([5])", "expected": "False", "isHidden": true }
      ]
    }
  ],
  "optimizations": [
    {
      "id": "m4_opt_1",
      "title": "Optimization Challenge: Avoiding List Count",
      "task": [
        "If you use the 'count occurrences' list block inside a loop, your program will slow down to O(n²) because it has to re-scan the entire list every time! Let's build a tally dictionary instead.",
        "",
        "Optimization Guidelines:",
        "1. Eradicate any blocks that use `count()` inside of a loop.",
        "2. Create an empty dictionary from the 'Dictionaries (Built-in Type)' tab.",
        "3. Loop through the dataset once, checking if the item is in the dictionary. If it isn't, set it to 1. If it is, add 1 to it.",
        "4. This drops the runtime to a blazing fast O(n) limit.",
        "",
        "Sample Outputs:",
        "optimize_frequency([10, 20, 10, 30]) → {10: 2, 20: 1, 30: 1}",
        "optimize_frequency([1, 1, 1]) → {1: 3}"
      ],
      "targetTime": "O(n)",
      "targetSpace": "O(n)",
      "templateUrl": "/data/optimizations/m4-4.1.json",
      "testCasesPool": [
        { "call": "optimize_frequency([10, 20, 10, 30])", "expected": "{10: 2, 20: 1, 30: 1}", "isHidden": false },
        { "call": "optimize_frequency([1, 1, 1])", "expected": "{1: 3}", "isHidden": false },
        { "call": "optimize_frequency([])", "expected": "{}", "isHidden": false },
        { "call": "optimize_frequency([5, 6])", "expected": "{5: 1, 6: 1}", "isHidden": true },
        { "call": "optimize_frequency([7, 7, 8, 8])", "expected": "{7: 2, 8: 2}", "isHidden": true }
      ]
    },
    {
      "id": "m4_opt_2",
      "title": "Optimization Challenge: Stop Using 'In List'",
      "task": [
        "When you use a logic block to check if an item 'is in' a list, the computer has to search the entire list one by one (O(n)). But if you check if an item 'is in' a Set, the computer finds it instantly (O(1))!",
        "",
        "Optimization Guidelines:",
        "1. Look at your code and find where you are checking if an item is inside a normal list.",
        "2. Before the loop even starts, convert that list into a Set.",
        "3. Change your logic block to check if the item is in your new Set instead.",
        "4. This tiny change prevents O(n²) bottlenecks!",
        "",
        "Sample Outputs:",
        "optimize_unique([4, 1, 2, 1, 4]) → [1, 2, 4]",
        "optimize_unique([3, 3, 3]) → [3]"
      ],
      "targetTime": "O(n)",
      "targetSpace": "O(n)",
      "templateUrl": "/data/optimizations/m4-4.2.json",
      "testCasesPool": [
        { "call": "optimize_unique([4, 1, 2, 1, 4])", "expected": "[1, 2, 4]", "isHidden": false },
        { "call": "optimize_unique([3, 3, 3])", "expected": "[3]", "isHidden": false },
        { "call": "optimize_unique([10])", "expected": "[10]", "isHidden": false },
        { "call": "optimize_unique([])", "expected": "[]", "isHidden": true },
        { "call": "optimize_unique([9, 8, 7, 7, 8])", "expected": "[7, 8, 9]", "isHidden": true }
      ]
    },
    {
      "id": "m4_opt_3",
      "title": "Optimization Challenge: Saving Your Math (Memoization)",
      "task": [
        "In our Fibonacci recursive function, the computer calculates `fib(2)` and `fib(3)` dozens of times, doing the same exact math over and over! We can use a Dictionary as a 'memory pad' to save our answers.",
        "",
        "Optimization Guidelines:",
        "1. Our function `fib_optimized(n, memo)` has a dictionary parameter `memo` that starts empty.",
        "2. Before doing any math, check if `n` is already a key in the `memo` dictionary. If it is, just return `memo[n]` instantly!",
        "3. If it's not, do the recursive math as normal.",
        "4. But before you return the answer, save it! Set `memo[n] = answer`.",
        "5. This takes an impossibly slow O(2^n) algorithm and makes it a blazing fast O(n) algorithm.",
        "",
        "Sample Outputs:",
        "fib_optimized(5, {}) → 5",
        "fib_optimized(10, {}) → 55"
      ],
      "targetTime": "O(n)",
      "targetSpace": "O(n)",
      "templateUrl": "/data/optimizations/m4-4.3.json",
      "testCasesPool": [
        { "call": "fib_optimized(5, {})", "expected": "5", "isHidden": false },
        { "call": "fib_optimized(10, {})", "expected": "55", "isHidden": false },
        { "call": "fib_optimized(0, {})", "expected": "0", "isHidden": false },
        { "call": "fib_optimized(1, {})", "expected": "1", "isHidden": true },
        { "call": "fib_optimized(6, {})", "expected": "8", "isHidden": true }
      ]
    }
  ]
}